#+title: Emacs Configuration
#+startup: overview
#+property: header-args:emacs-lisp :tangle init.el :results silent :comments no

* このファイルについて

このファイルは、Emacs設定の原本となるLiterate Configurationです。

- 編集するファイル: =init.org=
- 自動生成されるファイル: =init.el=
- =init.el= は直接編集しない
- 設定は役割ごとの見出しに追加する

初回のみ、次のコマンドで =init.el= を生成します。

#+begin_example
M-x org-babel-tangle
#+end_example

以降は、このファイルを保存すると自動的に =init.el= を生成します。

パッケージを追加するときは、原則として1パッケージにつき1つの小見出しと
1つの =use-package= ブロックを作成します。

* 起動と設定基盤

Emacsの起動時に必要となる、生成ファイルの形式とパッケージ設定の基盤です。

** 生成されるinit.elのヘッダー

生成される =init.el= の先頭部分です。
#+begin_src emacs-lisp
;;; init.el --- Generated from init.org -*- lexical-binding: t; -*-

;;; Commentary:
;; このファイルは init.org から自動生成されています。
;; 直接編集せず、init.org を編集してください。

;;; Code:
#+end_src
** パッケージ管理とuse-package

Emacs 29以降では =use-package= が標準搭載されています。
MELPAをパッケージ取得先へ追加し、=use-package= の自動インストール機能を有効にします。
#+begin_src emacs-lisp
(require 'package)

;; MELPAで配布されているパッケージを利用する。
(add-to-list 'package-archives
             '("melpa" . "https://melpa.org/packages/")
             t)

(require 'use-package)
(require 'use-package-ensure)
#+end_src
*** use-packageの追加テンプレート

今後、パッケージの設定は基本的に次の形で追加します。

#+begin_example
(use-package package-name
  :ensure t
  :init
  ;; パッケージが読み込まれる前の設定
  :custom
  ;; Customize変数の設定
  :bind
  ;; キーバインド
  :hook
  ;; フック
  :config
  ;; パッケージが読み込まれた後の設定
  )
#+end_example

** Emacs本体の基本設定

Emacs標準の基本動作、ファイル保護、表示、編集支援をまとめて設定します。

=emacs= というパッケージを読み込むわけではなく、
Emacs本体の設定を =use-package= 形式で整理するために使用しています。
#+begin_src emacs-lisp
  (use-package emacs
    :ensure nil

    :init
    ;; カーソルを幅2pxの縦ラインにする。
    (setq-default cursor-type '(bar . 2))

    ;; Customizeが生成した設定をinit.elへ直接書き込ませない。
    ;; init.elはinit.orgから再生成されるため、別ファイルへ分離する。
    (setq custom-file
          (expand-file-name "custom.el" user-emacs-directory))

    (load custom-file t t)

    ;; バックアップファイルと自動保存ファイルを専用ディレクトリへ置く。
    (let ((backup-directory
           (expand-file-name "backups/" user-emacs-directory))
          (auto-save-directory
           (expand-file-name "auto-save/" user-emacs-directory)))

      (make-directory backup-directory t)
      (make-directory auto-save-directory t)

      (setq backup-directory-alist
            `(("." . ,backup-directory))

            auto-save-file-name-transforms
            `((".*" ,auto-save-directory t))))

    :custom
    ;; 起動画面を表示しない。
    (inhibit-startup-screen t)

    ;; *scratch*バッファの初期メッセージを表示しない。
    (initial-scratch-message nil)

    ;; yes/noではなくy/nで答えられるようにする。
    (use-short-answers t)

    ;; ビープ音を鳴らさない。
    (ring-bell-function #'ignore)

    ;; ファイル末尾に改行を入れる。
    (require-final-newline t)

    ;; カーソル移動によるスクロールを1行ずつにする。
    (scroll-conservatively most-positive-fixnum)

    ;; インデント済みの位置でTABを押した場合は補完を実行する。
    (tab-always-indent 'complete)

    :config
    ;; Repeat対応コマンドを、短いキーで連続実行できるようにする。
    (repeat-mode 1)

    ;; 選択範囲がある状態で入力すると、選択範囲を置換する。
    (delete-selection-mode 1)

    ;; 括弧を自動的に補完する。
    (electric-pair-mode 1)

    ;; 対応する括弧を強調表示する。
    (show-paren-mode 1)

    ;; 行番号と列番号を表示する。
    (global-display-line-numbers-mode 1)
    (column-number-mode 1)

    ;; GUIのツールバーとスクロールバーを非表示にする。
    ;; メニューバーはEmacsを覚えるのに役立つため残す。
    (when (fboundp 'tool-bar-mode)
      (tool-bar-mode -1))

    (when (fboundp 'scroll-bar-mode)
      (scroll-bar-mode -1)))
#+end_src
** Pixel Scroll：滑らかなスクロール

トラックパッドやマウスではピクセル単位のスクロールを使用します。
PageUp・PageDownでは、素早く動き始めて徐々に減速する
ease-out-cubicのアニメーションを使用します。

#+begin_src emacs-lisp
  (use-package pixel-scroll
    :ensure nil

    :custom
    ;; PageUp・PageDownを補間スクロールの対象にする。
    (pixel-scroll-precision-interpolate-page t)

    ;; PageUp・PageDownのアニメーション時間。
    (pixel-scroll-precision-interpolation-total-time 0.15)

    :config
    (pixel-scroll-precision-mode 1)

    (defun my/pixel-scroll-ease-out-cubic (progress)
      "PROGRESSにease-out-cubicを適用する。"
      (- 1.0
         (expt (- 1.0 progress) 3)))

    (defun my/pixel-scroll-interpolate (delta)
      "DELTAピクセルをイージング付きでスクロールする。"
      (let ((progress 0.0)
            (elapsed 0.0)
            (last-time (float-time))
            (last-delta 0.0)
            (total-time
             pixel-scroll-precision-interpolation-total-time)
            (interval
             pixel-scroll-precision-interpolation-between-scroll))

        (condition-case error-data
            (while-no-input
              (while (< progress 1.0)
                (redisplay t)
                (sleep-for interval)

                (let ((current-time (float-time)))
                  (setq elapsed
                        (+ elapsed (- current-time last-time))
                        last-time current-time
                        progress
                        (min 1.0 (/ elapsed total-time)))

                  (let* ((eased-progress
                          (my/pixel-scroll-ease-out-cubic progress))
                         (absolute-delta
                          (* eased-progress delta))
                         (relative-delta
                          (abs
                           (round
                            (- absolute-delta last-delta)))))

                    (setq last-delta absolute-delta)

                    (unless (zerop relative-delta)
                      (if (< delta 0)
                          (pixel-scroll-precision-scroll-down
                           relative-delta)
                        (pixel-scroll-precision-scroll-up
                         relative-delta)))))))

          ((beginning-of-buffer end-of-buffer)
           (message "%s"
                    (error-message-string error-data))))))

    (defun my/pixel-scroll-page-down ()
      "ease-out-cubicで1ページ下へスクロールする。"
      (interactive)
      (my/pixel-scroll-interpolate
       (- (window-text-height nil t))))

    (defun my/pixel-scroll-page-up ()
      "ease-out-cubicで1ページ上へスクロールする。"
      (interactive)
      (my/pixel-scroll-interpolate
       (window-text-height nil t)))

    (defun my/pixel-scroll-half-page-down ()
      "ease-out-cubicで半ページ下へスクロールする。"
      (interactive)
      (my/pixel-scroll-interpolate
       (- (max 1
               (/ (window-text-height nil t) 2)))))

    (defun my/pixel-scroll-half-page-up ()
      "ease-out-cubicで半ページ上へスクロールする。"
      (interactive)
      (my/pixel-scroll-interpolate
       (max 1
            (/ (window-text-height nil t) 2))))

    ;; pixel-scroll-precision-mode標準のPageUp・PageDownを上書きする。
    (keymap-set pixel-scroll-precision-mode-map
                "<next>"
                #'my/pixel-scroll-page-down)

    (keymap-set pixel-scroll-precision-mode-map
                "<prior>"
                #'my/pixel-scroll-page-up)
    
    ;; Command＋PageDown・PageUpでは半ページずつスクロールする。
    (keymap-set pixel-scroll-precision-mode-map
                "s-<next>"
                #'my/pixel-scroll-half-page-down)

    (keymap-set pixel-scroll-precision-mode-map
                "s-<prior>"
                #'my/pixel-scroll-half-page-up)
    )
                    #+end_src
** Dashboard：起動時の作業ハブ

最近開いたファイル、プロジェクト、ブックマークを表示し、
各作業場所へ移動するための入口として使用します。

#+begin_src emacs-lisp
  (use-package dashboard
    :ensure t

    :custom
    ;; 内容を画面中央に表示する。
    (dashboard-center-content t)

    ;; Emacs標準のproject.elからプロジェクトを取得する。
    (dashboard-projects-backend 'project-el)

    ;; ダッシュボードへ表示する項目。
    (dashboard-items
     '((projects  . 5)
       (recents   . 5)
       (bookmarks . 5)))

    ;; 項目間を循環して移動できるようにする。
    (dashboard-navigation-cycle t)

    :config
    ;; Emacs起動時にDashboardを表示する。
    (dashboard-setup-startup-hook))
#+end_src

* macOSとの連携

macOS版Emacs固有の修飾キーと、GUI起動時のシェル環境を設定します。

** 修飾キー

OptionキーをEmacsのMetaキーとして使用します。
CommandキーはSuperキーとして使用し、macOS風のショートカットへ割り当てます。
#+begin_src emacs-lisp
(when (eq window-system 'mac)
  ;; OptionをMetaとして使う。
  (setq mac-option-modifier 'meta)

  ;; Commandを独立したSuper修飾キーとして使う。
  (setq mac-command-modifier 'super))
#+end_src
** シェル環境の読み込み

GUI版Emacsから、ターミナルと同じPATHを利用できるようにします。
#+begin_src emacs-lisp
(use-package exec-path-from-shell
  :ensure t
  :if (eq window-system 'mac)

  :init
  (exec-path-from-shell-initialize))
#+end_src
* 状態の保存とファイル同期

Emacsを終了・再起動しても引き継ぎたい状態と、外部で変更されたファイルの同期を設定します。

** セッションをまたいで保存する状態

*** ミニバッファの履歴

検索履歴や実行したコマンドなどを、Emacs終了後も保存します。
#+begin_src emacs-lisp
(use-package savehist
  :ensure nil

  :custom
  (history-length 200)

  :init
  (savehist-mode 1))
#+end_src
*** 最近開いたファイル

最近開いたファイルの一覧を保存します。
#+begin_src emacs-lisp
(use-package recentf
  :ensure nil

  :custom
  (recentf-max-saved-items 100)

  :init
  (recentf-mode 1))
#+end_src
*** カーソル位置

ファイルを開き直したとき、前回のカーソル位置へ戻ります。
#+begin_src emacs-lisp
(use-package saveplace
  :ensure nil

  :init
  (save-place-mode 1))
#+end_src
** 外部変更の自動反映

外部プログラムやGitなどによってファイルが変更されたとき、
Emacs上のバッファも自動的に更新します。
#+begin_src emacs-lisp
(use-package autorevert
  :ensure nil

  :custom
  (global-auto-revert-non-file-buffers t)

  :init
  (global-auto-revert-mode 1))
#+end_src
* ファイルとディレクトリ

ファイルとディレクトリの表示、移動、コピー、削除、
階層表示、プレビューに関する設定をまとめます。

** Dired：標準のファイル管理
#+begin_src emacs-lisp
  (defface my-dired-file-face
    '((t (:inherit default)))
    "Face used for regular files in Dired."
    :group 'dired-faces)

  (defun my-dired-fontify-regular-files ()
    "Apply `my-dired-file-face' to regular files."
    (font-lock-add-keywords
     nil
     (list
      (list
       ;; 権限表示の先頭が「-」なら通常ファイル。
       (concat dired-re-maybe-mark
               dired-re-inode-size
               "-")
       '(".+" (dired-move-to-filename) nil
         (0 'my-dired-file-face))))
     'append))

  (require 'dired)
  (require 'project)

  (defun my-dired-open-picker (directory &optional switches)
    "DIRECTORYを、一時的なDiredとして別ウィンドウに開く。"
    (let* ((origin-window (selected-window))
           (origin-marker
            (with-current-buffer (window-buffer origin-window)
              (copy-marker (window-point origin-window))))
           ;; Diredを開く前のウィンドウ構成を保存する。
           (window-config (current-window-configuration)))

      ;; ウィンドウを分割、または既存の別ウィンドウを使ってDiredを開く。
      (dired-other-window directory switches)

      ;; Diredを表示しているウィンドウに、
      ;; 戻り先、正確なカーソル位置、元のウィンドウ構成を記録する。
      (set-window-parameter
       (selected-window)
       'my-dired-origin-window
       origin-window)

      (set-window-parameter
       (selected-window)
       'my-dired-origin-marker
       origin-marker)

      (set-window-parameter
       (selected-window)
       'my-dired-window-config
       window-config)))

  (defun my-dired-other-window (directory &optional switches)
    "DIRECTORYを選択し、一時的なDiredとして別ウィンドウに開く。"
    (interactive (dired-read-dir-and-switches ""))
    (my-dired-open-picker directory switches))

  (defun my-project-dired-other-window ()
    "プロジェクトルートを、一時的なDiredとして別ウィンドウに開く。"
    (interactive)
    (let* ((project (project-current t))
           (root    (project-root project)))
      (my-dired-open-picker root)))

  (defun my-dired--clear-picker-state (window)
    "WINDOWに保存した一時Dired用の情報を削除する。"
    (let ((origin-marker
           (window-parameter window
                             'my-dired-origin-marker)))
      (when (markerp origin-marker)
        (set-marker origin-marker nil)))

    (set-window-parameter window 'my-dired-origin-window nil)
    (set-window-parameter window 'my-dired-origin-marker nil)
    (set-window-parameter window 'my-dired-window-config nil))

  (defun my-dired--display-file-after-restore
      (file-buffer new-window-p)
    "復元後の画面へFILE-BUFFERを表示し、表示先ウィンドウを返す。
NEW-WINDOW-Pが非nilなら、新しいウィンドウを作成して表示する。"
    (if (not new-window-p)
        (progn
          (switch-to-buffer file-buffer)
          (selected-window))

      (let ((new-window
             (split-window-sensibly (selected-window))))
        (if (window-live-p new-window)
            (progn
              (set-window-buffer new-window file-buffer)

              ;; 分割時に継承した作業バッファを、新しいウィンドウの
              ;; Tab Lineと標準バッファ履歴へ持ち込まない。
              (set-window-prev-buffers new-window nil)
              (set-window-next-buffers new-window nil)
              (set-window-parameter
               new-window
               'tab-line-buffers
               (list file-buffer))

              (select-window new-window)
              (force-mode-line-update t)
              new-window)

          ;; フレームが小さく分割できない場合も、ファイルは開けるようにする。
          (message
           "新しいウィンドウを作成できないため、現在のウィンドウで開きます")
          (switch-to-buffer file-buffer)
          (selected-window)))))

  (defun my-dired--find-file-and-restore (new-window-p)
    "一時Diredで選択したファイルを開き、元の画面へ戻す。
NEW-WINDOW-Pが非nilなら、新しいウィンドウへファイルを表示する。"
    (let* ((dired-window (selected-window))
           (origin-window
            (window-parameter dired-window
                              'my-dired-origin-window))
           (origin-marker
            (window-parameter dired-window
                              'my-dired-origin-marker))
           (window-config
            (window-parameter dired-window
                              'my-dired-window-config))
           (file (dired-get-file-for-visit)))

      ;; 一時Dired以外では、Emacs標準のDired動作を維持する。
      (if (not (window-configuration-p window-config))
          (if new-window-p
              (dired-find-file-other-window)
            (dired-find-file))

        ;; ディレクトリを選んだ場合は、Dired内で移動を続ける。
        (if (file-directory-p file)
            (dired-find-alternate-file)

          ;; ファイルは、まず表示せずにバッファへ読み込む。
          ;; 読み込みに失敗した場合はDiredを閉じない。
          (let ((file-buffer  (find-file-noselect file))
                (dired-buffer (current-buffer))
                (origin-marker-copy
                 (and (markerp origin-marker)
                      (marker-position origin-marker)
                      (copy-marker origin-marker)))
                destination-window)

            (unwind-protect
                (progn
                  ;; 一時Diredの状態を解除する。
                  (my-dired--clear-picker-state dired-window)

                  ;; Diredを開く前のウィンドウ構成へ戻す。
                  (set-window-configuration window-config)

                  ;; 元の作業ウィンドウを選択する。
                  (when (window-live-p origin-window)
                    (select-window origin-window))

                  ;; 選択した方法でファイルを表示する。
                  (setq destination-window
                        (my-dired--display-file-after-restore
                         file-buffer
                         new-window-p))

                  ;; 新しいウィンドウへ開いた場合は、元の作業ウィンドウと
                  ;; 正確なカーソル位置をナビゲーション履歴へ登録する。
                  (when (and new-window-p
                             (window-live-p origin-window)
                             (window-live-p destination-window)
                             (not (eq origin-window
                                      destination-window))
                             origin-marker-copy
                             (fboundp
                              'my/navigation-history-register-window-transition))
                    (my/navigation-history-register-window-transition
                     origin-window
                     origin-marker-copy
                     destination-window))

                  ;; 一時的に使ったDiredバッファを削除する。
                  (when (buffer-live-p dired-buffer)
                    (kill-buffer dired-buffer)))

              (when (markerp origin-marker-copy)
                (set-marker origin-marker-copy nil))))))))

  (defun my-dired-find-file-and-restore ()
    "選択したファイルを元の作業ウィンドウで開き、Diredを終了する。"
    (interactive)
    (my-dired--find-file-and-restore nil))

  (defun my-dired-find-file-new-window-and-restore ()
    "選択したファイルを新しいウィンドウで開き、Diredを終了する。"
    (interactive)
    (my-dired--find-file-and-restore t))

  (defun my-dired-quit-and-restore ()
    "一時Diredをキャンセルし、元のウィンドウ構成へ戻す。"
    (interactive)
    (let* ((dired-window  (selected-window))
           (origin-window
            (window-parameter dired-window
                              'my-dired-origin-window))
           (window-config
            (window-parameter dired-window
                              'my-dired-window-config)))

      ;; 通常のDiredなら、標準の終了動作を行う。
      (if (not (window-configuration-p window-config))
          (quit-window)

        (let ((dired-buffer (current-buffer)))
          (my-dired--clear-picker-state dired-window)
          (set-window-configuration window-config)

          (when (window-live-p origin-window)
            (select-window origin-window))

          (when (buffer-live-p dired-buffer)
            (kill-buffer dired-buffer))))))

  (defun my-dired-disable-line-numbers ()
    "Diredでは行番号を表示しない。"
    (display-line-numbers-mode -1))

  (use-package dired
    :ensure nil

    :hook
    ;; 所有者、権限、日時などを最初は隠して、一覧をすっきり表示する。
    ((dired-mode . dired-hide-details-mode)
     (dired-mode . my-dired-fontify-regular-files)
     (dired-mode . my-dired-disable-line-numbers))

    :bind
    (("C-x d" . my-dired-other-window)
     ("s-b" . my-project-dired-other-window))

    :config
    ;; RET、f、eなど、`dired-find-file'を呼ぶキーをまとめて置き換える。
    (define-key dired-mode-map
                [remap dired-find-file]
                #'my-dired-find-file-and-restore)

    ;; Cmd+RETでは、元の画面を復元してから新しいウィンドウで開く。
    (define-key dired-mode-map
                (kbd "s-<return>")
                #'my-dired-find-file-new-window-and-restore)

    ;; qでキャンセルした場合も、元の画面へ完全に戻す。
    (define-key dired-mode-map
                (kbd "q")
                #'my-dired-quit-and-restore)

    :custom
    ;; 2つのDiredを左右に表示している場合、
    ;; コピー・移動先の初期値をもう一方のディレクトリにする。
    (dired-dwim-target t)

    ;; ディレクトリを移動してもDiredバッファを大量に残さない。
    (dired-kill-when-opening-new-dired-buffer t)

    ;; ディレクトリの再帰コピーは確認なしで行う。
    (dired-recursive-copies 'always)

    ;; 再帰削除では最上位で一度確認する。
    (dired-recursive-deletes 'top)

    ;; 削除したファイルを直接消さず、macOSのゴミ箱へ移動する。
    (delete-by-moving-to-trash t)

    ;; 隠しファイルは表示するが、`.` と `..` は表示しない。
    ;(dired-listing-switches "-Al")

    :custom-face
    ;; 通常ファイル
    (my-dired-file-face
     ((t (:inherit font-lock-string-face))))

    ;; ディレクトリ
    (dired-directory
     ((t (:inherit font-lock-type-face
                   :weight bold)))))
#+end_src
** Nerd Icons：ファイルアイコンの基盤
#+begin_src emacs-lisp
(use-package nerd-icons
  :ensure t)
#+end_src

** Nerd Icons Dired：Diredのアイコン表示
#+begin_src emacs-lisp
  (defun my-nerd-icons-dired-refresh ()
    "Diredの内容が部分的に変化した後、Nerd Iconsを再描画する。"
    (when (and (bound-and-true-p nerd-icons-dired-mode)
               (fboundp 'nerd-icons-dired--refresh))
      (nerd-icons-dired--refresh)))

  (use-package nerd-icons-dired
    :ensure t
    :after (dired nerd-icons)

    :hook
    (dired-mode . nerd-icons-dired-mode))
#+end_src

** Dired Subtree：ディレクトリのツリー展開
#+begin_src emacs-lisp
  (use-package dired-subtree
    :ensure t
    :after dired

    :bind
    (:map dired-mode-map
          ;; カーソル位置のディレクトリを展開・折り畳みする。
          ("<tab>" . dired-subtree-toggle)

          ;; サブツリーの展開状態を段階的に切り替える。
          ("<backtab>" . dired-subtree-cycle))

    :custom
    ;; 階層ごとに付けられる背景色を使用しない。
    (dired-subtree-use-backgrounds nil)

    ;; 罫線なしで、階層ごとに4文字インデントする。
    (dired-subtree-line-prefix "    ")

    ;; 罫線へサブツリー用のfaceを継承させない。
    (dired-subtree-line-prefix-face nil)

    :config
    ;; dired-subtreeはDiredバッファの途中へ直接行を挿入するため、
    ;; 挿入後にNerd Iconsを再描画する。
    (add-hook 'dired-subtree-after-insert-hook
              #'my-nerd-icons-dired-refresh)

    ;; 折り畳み後に残ったアイコン用overlayも整理する。
    (add-hook 'dired-subtree-after-remove-hook
              #'my-nerd-icons-dired-refresh))
#+end_src

** Diff HL：Git差分の表示
#+begin_src emacs-lisp
  (use-package diff-hl
    :ensure t

    :hook
    ;; 通常のファイルでは、変更された行をウィンドウ左端に表示する。
    ((prog-mode . diff-hl-mode)
     (text-mode . diff-hl-mode)

     ;; Diredでは、Git状態に応じてファイル単位の印を表示する。
     (dired-mode . diff-hl-dired-mode))

    :custom
    ;; .gitignore対象のファイルまでは表示しない。
    ;; 未追跡ファイルは引き続き表示される。
    (diff-hl-dired-extra-indicators nil)

    :config
    ;; dired-subtreeで後から挿入されたファイルについても、
    ;; Git状態を再取得して表示する。
    (with-eval-after-load 'dired-subtree
      (add-hook 'dired-subtree-after-insert-hook
                #'diff-hl-dired-update)))
#+end_src
* ウィンドウとバッファの移動

作業画面全体と、各ウィンドウ内のバッファをタブとして切り替える設定です。
** Window Move：次に実行したコマンドが表示するバッファの行き先を予約する

#+begin_src emacs-lisp
  (use-package windmove
    :ensure nil

    :bind
    (("C-x <left>"  . windmove-display-left)
     ("C-x <right>" . windmove-display-right)
     ("C-x <up>"    . windmove-display-up)
     ("C-x <down>"  . windmove-display-down)))
#+end_src

** Tab Bar：作業画面全体の切り替え

画面分割を含む作業画面全体を、タブとして切り替えます。

#+begin_src emacs-lisp
  (defun my/tab-bar-reset-dashboard-tab (_tab)
    "新しく作成されたDashboardタブを、Dashboardだけの状態にする。"
    (when (derived-mode-p 'dashboard-mode)
      (let ((window (selected-window)))
        ;; Tab Lineが参照するウィンドウ固有の履歴を初期化する。
        (set-window-prev-buffers window nil)
        (set-window-next-buffers window nil)

        ;; 独自Tab Lineの固定順もDashboardだけにする。
        (set-window-parameter
         window
         'tab-line-buffers
         (list (window-buffer window))))

      ;; 新しいTab BarのBufferlo一覧を、
      ;; 現在表示中のDashboardだけにする。
      (bufferlo-clear)

      ;; Tab Lineを再描画する。
      (force-mode-line-update t)))

  (defun my/close-window-or-tab ()
    "複数ウィンドウなら現在のウィンドウを閉じる。
    1ウィンドウかつ複数Tab Barなら現在のTab Barを閉じる。
    最後のウィンドウかつ最後のTab Barでは何もしない。"
    (interactive)
    (cond
     ;; ミニバッファを除く通常ウィンドウが複数ある場合。
     ((not (one-window-p t))
      (delete-window))

     ;; 通常ウィンドウが1つで、Tab Barが複数ある場合。
     ((> (length
          (funcall tab-bar-tabs-function))
         1)
      (tab-close))

     ;; 最後のウィンドウかつ最後のTab Bar。
     (t
      (message "最後のウィンドウとTab Barは閉じません"))))

  (use-package tab-bar
    :ensure nil

    :custom
    ;; 新しいTab BarはDashboardから始める。
    (tab-bar-new-tab-choice #'dashboard-open)

    ;; 新しいタブを作る「＋」ボタンを表示しない。
    (tab-bar-new-button-show nil)

    ;; 各タブの「×」ボタンを表示しない。
    (tab-bar-close-button-show nil)

    :config
    ;; Emacs標準のTab Barを有効にする。
    (tab-bar-mode 1)

    ;; Ctrl+PageDown：右隣のTab Barへ移動する。
    ;; 右端では左端へ戻り、循環する。
    (keymap-global-set "C-<next>" #'tab-next)

    ;; Ctrl+PageUp：左隣のTab Barへ移動する。
    ;; 左端では右端へ戻り、循環する。
    (keymap-global-set "C-<prior>" #'tab-previous)

    ;; 新規タブの構築が完了したあとに状態を整理する。
    (add-hook
     'tab-bar-tab-post-open-functions
     #'my/tab-bar-reset-dashboard-tab))
#+end_src

** Bufferlo：Tab Barごとのバッファ管理

各Tab Barが独立したバッファ一覧を持つようにします。

#+begin_src emacs-lisp
  (use-package bufferlo
    :ensure t
    :demand t

    :init
    ;; next-bufferとprevious-bufferも現在のTab Bar内に限定する。
    ;; bufferlo-modeを有効にする前に設定する必要がある。
    (setq bufferlo-prefer-local-buffers 'tabs)

    :bind
    ;; 現在のTab Barに属するバッファから選択する。
    ;; Prefix引数を付けると、Emacs全体のバッファから選択できる。
    (("C-x b" . bufferlo-switch-to-buffer))

    :config
    ;; フレーム・Tab Barごとのローカルバッファ管理を有効にする。
    (bufferlo-mode 1))
#+end_src
** Tab Line：ウィンドウ内のバッファ切り替え

各ウィンドウで表示したバッファを、タブとして切り替えます。

| キー         | 操作                 |
|--------------+----------------------|
| =Cmd+Option+←= | 左隣のTab Lineへ移動 |
| =Cmd+Option+→= | 右隣のTab Lineへ移動 |

どちらも端まで移動すると、反対側へ戻って循環します。

Tab Lineのバッファ名は並び順を維持し、
新しく表示したバッファだけを右端へ追加します。
#+begin_src emacs-lisp
  (use-package tab-line
    :ensure nil
    :after bufferlo

    :init
    (require 'seq)

    ;; Tab Lineに表示しないバッファ名。
    (defconst my/tab-line-excluded-buffer-names
      '("*scratch*")
      "Tab Lineに表示しないバッファ名の一覧。")

    (defun my/tab-line-buffer-visible-p (buffer)
      "BUFFERをTab Lineに表示するとき、非nilを返す。"
      (and
       ;; 削除済みのバッファは表示しない。
       (buffer-live-p buffer)

       ;; ミニバッファはTab Lineへ表示しない。
       (not (minibufferp buffer))

       ;; 通常のScratchと、Bufferloが作るタブ専用Scratchを表示しない。
       (not
        (string-prefix-p
         "*scratch*"
         (buffer-name buffer)))

       ;; 表示しないバッファはここに定義する。
       (not
        (with-current-buffer buffer
          (derived-mode-p
           'dired-mode
           'magit-mode
           'vterm-mode)))))

    (defun my/tab-line-tabs ()
      "不要なバッファを除外し、固定順でTab Lineへ表示する。"
      (let* (;; このウィンドウで保存している、前回までの並び順。
             (old-buffers
              (seq-filter
               #'my/tab-line-buffer-visible-p
               (window-parameter nil 'tab-line-buffers)))

             ;; 現在、このウィンドウに関連付いているバッファ。
             (current-buffers
              (seq-filter
               #'my/tab-line-buffer-visible-p
               (tab-line-tabs-window-buffers)))

             ;; 以前からあるバッファは、保存済みの順番を維持する。
             (kept-buffers
              (seq-filter
               (lambda (buffer)
                 (memq buffer current-buffers))
               old-buffers))

             ;; 新しく表示されたバッファは、右端へ追加する。
             (added-buffers
              (seq-remove
               (lambda (buffer)
                 (memq buffer kept-buffers))
               current-buffers))

             (buffers
              (append kept-buffers added-buffers)))

        ;; ウィンドウごとのTab Lineの並び順として保存する。
        (set-window-parameter nil 'tab-line-buffers buffers)

        buffers))

    ;; Tab Barを切り替えても、ウィンドウごとの並び順を保持する。
    (add-to-list
     'window-persistent-parameters
     '(tab-line-buffers . t))

    :custom
    ;; Tab Lineへ表示するバッファを独自に絞り込む。
    (tab-line-tabs-function #'my/tab-line-tabs)

    ;; 右端・左端へ到達したら、反対側へ戻って循環する。
    (tab-line-switch-cycling t)

    ;; 各タブの閉じるボタンを表示しない。
    (tab-line-close-button-show nil)

    :config
    ;; すべてのウィンドウでTab Lineを有効にする。
    (global-tab-line-mode 1)

    ;; Cmd+Option+←：左隣のTab Lineへ移動する。
    (keymap-global-set
     "M-s-<left>"
     #'tab-line-switch-to-prev-tab)

    ;; Cmd+Option+→：右隣のTab Lineへ移動する。
    (keymap-global-set
     "M-s-<right>"
     #'tab-line-switch-to-next-tab))
#+end_src
* Org mode

Org mode本体、Capture、Agenda、表示補助をまとめます。

** 管理対象ファイル

Org関連の設定やコマンドから参照するファイルパスを定義します。

*** 個人作業管理ファイル

個人の気づきや作業状態を管理するOrgファイルの場所を定義します。

- =inbox.org=: 未整理の気づきや疑問
- =cockpit.org=: 状態を持つ作業項目
#+begin_src emacs-lisp
(defconst my/task-org-directory
  (expand-file-name "~/org/cockpit/")
  "個人の作業管理用Orgファイルを保存するディレクトリ。")

(defconst my/task-inbox-file
  (expand-file-name "inbox.org" my/task-org-directory)
  "未整理の気づきを保存するOrgファイル。")

(defconst my/task-cockpit-file
  (expand-file-name "cockpit.org" my/task-org-directory)
  "状態を持つ作業項目を保存するOrgファイル。")
#+end_src
*** Emacs設定ファイル

=init.org= を開くコマンド、=init.el= を生成するコマンド、
生成した設定を再読み込みするコマンドを定義します。

- =C-c e i=: init.orgを開く
- =C-c e t=: init.orgからinit.elを生成する
- =C-c e r=: init.elを再読み込みする
#+begin_src emacs-lisp
(defconst my/init-org-file
  (expand-file-name "init.org" user-emacs-directory)
  "Emacs設定の原本となるOrgファイル。")

(defconst my/init-el-file
  (expand-file-name "init.el" user-emacs-directory)
  "init.orgから生成されるEmacs Lispファイル。")

(defun my/open-init-org ()
  "Emacs設定ファイルinit.orgを開く。"
  (interactive)
  (find-file my/init-org-file))

(defun my/tangle-init-org ()
  "init.orgからinit.elを生成する。"
  (interactive)
  (require 'org)
  (require 'ob-tangle)

  (org-babel-tangle-file
   my/init-org-file
   my/init-el-file
   "emacs-lisp")

  (message "init.orgからinit.elを生成しました"))

(defun my/reload-init-file ()
  "生成済みのinit.elを再読み込みする。"
  (interactive)
  (if (file-exists-p my/init-el-file)
      (progn
        (load-file my/init-el-file)
        (message "init.elを再読み込みしました"))
    (user-error
     "init.elが存在しません。先にC-c e tで生成してください")))
#+end_src
** Org mode本体の設定

Orgファイルの初期表示、TODO状態、Agenda対象ファイルを設定します。
#+begin_src emacs-lisp
  (use-package org
    :ensure nil
    :defer t

    :init
    ;; Org modeでもShift+矢印による範囲選択を使用する。
    ;;
    ;; org-replace-disputed-keysはOrgのShift+矢印操作を別キーへ移すため、
    ;; Orgが読み込まれる前に設定する。
    (setq org-support-shift-select t
          org-replace-disputed-keys t)

    :custom
    ;; Orgファイルを開いたとき、見出しを折りたたんで表示する。
    (org-startup-folded 'content)

    ;; アウトラインレベルに応じて視覚的にインデントする。
    (org-startup-indented t)

    ;; =などを非表示にする
    (org-hide-emphasis-markers t)

    ;; DONEへ変更した日時を記録する。
    (org-log-done 'time)

    :config
    ;; InboxとCockpitをAgendaの検索対象にする。
    (setq org-agenda-files
          (list my/task-inbox-file
                my/task-cockpit-file)))
#+end_src
** 作業項目の登録

*** Org Capture

未整理の気づきは =inbox.org= へ追加します。
作業状態として管理する項目は =cockpit.org= へ追加します。

=C-c c= でCaptureメニューを開きます。

| 操作 | 追加先      | 初期状態 |
|------+-------------+----------|
| =i=    | =inbox.org=   | =TODO=     |
| =c t=  | =cockpit.org= | =TODO=     |
| =c n=  | =cockpit.org= | =NEXT=     |
| =c d=  | =cockpit.org= | =DOING=    |
| =c h=  | =cockpit.org= | =HOLD=     |
| =c w=  | =cockpit.org= | =WAIT=     |
#+begin_src emacs-lisp
(use-package org-capture
  :ensure nil
  :after org

  :config
  (setq org-capture-templates
        `(
          ;; 未整理の気づきはInboxへ追加する。
          ("i" "Inbox" entry
           (file+headline ,my/task-inbox-file "Inbox")
           "* TODO %?\n:PROPERTIES:\n:CAPTURED: %U\n:END:\n%a")

          ;; Cockpit用テンプレートのグループ。
          ("c" "Cockpit")

          ;; まだ着手順を決めていない作業項目。
          ("ct" "TODO" entry
           (file+headline ,my/task-cockpit-file "Work Items")
           "* TODO %?\n- 次: \n%a")

          ;; 次に着手できる作業項目。
          ("cn" "NEXT" entry
           (file+headline ,my/task-cockpit-file "Work Items")
           "* NEXT %?\n- 次: \n%a")

          ;; 現在作業中の項目。
          ("cd" "DOING" entry
           (file+headline ,my/task-cockpit-file "Work Items")
           "* DOING %?\n- 今: \n- 次: \n%a")

          ;; 差し込みなどで中断する項目。
          ("ch" "HOLD" entry
           (file+headline ,my/task-cockpit-file "Work Items")
           "* HOLD %?\n- 中断地点: \n- 再開時の一手: \n- 中断理由: \n%a")

          ;; 外部条件や回答を待つ項目。
          ("cw" "WAIT" entry
           (file+headline ,my/task-cockpit-file "Work Items")
           "* WAIT %?\n- 待っているもの: \n- 解除条件: \n- 次に確認する時点: \n- 解除後の一手: \n%a"))))
#+end_src
** 作業項目の移動

=inbox.org= の項目上で =C-c w= を押すと、
現在項目を =cockpit.org= の =Work Items= へ移動します。

本文や子見出しにカーソルがある場合も、Inbox直下の項目全体を移動します。
#+begin_src emacs-lisp
(defun my/org-refile-location (file headline)
  "FILE内のHEADLINEを、`org-refile'用の移動先として返す。"
  (let* ((buffer (find-file-noselect file))
         (target-file (buffer-file-name buffer))
         (position
          (with-current-buffer buffer
            (org-with-wide-buffer
              (goto-char (point-min))
              (or (org-find-exact-headline-in-buffer headline)
                  (user-error
                   "%sに見出し「%s」がありません"
                   file
                   headline))))))
    (list headline target-file nil position)))

(defun my/save-task-org-buffers ()
  "変更されたInboxとCockpitのバッファを保存する。"
  (dolist (file (list my/task-inbox-file
                      my/task-cockpit-file))
    (let ((buffer (find-buffer-visiting file)))
      (when buffer
        (with-current-buffer buffer
          (when (buffer-modified-p)
            (save-buffer)))))))

(defun my/move-inbox-item-to-work-items ()
  "Inboxの現在項目をCockpitのWork Itemsへ移動する。"
  (interactive)
  (unless (and buffer-file-name
               (file-equal-p buffer-file-name
                             my/task-inbox-file))
    (user-error "このコマンドはinbox.orgでのみ使用できます"))

  (org-back-to-heading t)

  ;; 子見出し内から実行した場合も、Inbox直下の項目全体を対象にする。
  (while (> (org-outline-level) 2)
    (org-up-heading-safe))

  (unless (and (= (org-outline-level) 2)
               (save-excursion
                 (org-up-heading-safe)
                 (equal (org-get-heading t t t t)
                        "Inbox")))
    (user-error "Inbox直下の項目で実行してください"))

  (org-refile
   nil
   nil
   (my/org-refile-location
    my/task-cockpit-file
    "Work Items"))

  (my/save-task-org-buffers)
  (message "Inbox項目をCockpitのWork Itemsへ移動しました"))

(with-eval-after-load 'org
  (keymap-set
   org-mode-map
   "C-c w"
   #'my/move-inbox-item-to-work-items))
#+end_src
** 作業状態の一覧表示

*** Cockpit Agenda

=C-c a c= で、Cockpitの作業状態を一覧表示します。
表示上の分類はTODO状態から自動生成します。

| 表示      | 対象状態            |
|-----------+---------------------|
| Active    | =DOING=               |
| Next      | =NEXT=                |
| Suspended | =HOLD=                |
| Waiting   | =WAIT=                |
| Planned   | =TODO=のCockpit項目 |
| Inbox     | =TODO=のInbox項目   |

Agenda項目上では、次のキーで表示順に隣接するレーンへ移動します。
移動後は同じ項目へカーソルも追従するため、
Cmdを押したまま上下を連続入力できます。

| キー      | 操作                              |
|-----------+-----------------------------------|
| =Cmd+↑=     | 1つ上のワークフローレーンへ移動 |
| =Cmd+↓=     | 1つ下のワークフローレーンへ移動 |
| =Option+↑=  | Agenda標準の行移動               |
| =Option+↓=  | Agenda標準の行移動               |

Cockpit内の移動ではTODO状態を変更し、
PlannedとInboxの境界では項目をファイル間で移動します。
#+begin_src emacs-lisp
(require 'cl-lib)

(defconst my/org-agenda-workflow-lanes
  `((:name "Active"
     :file ,my/task-cockpit-file
     :todo "DOING"
     :parent "Work Items")
    (:name "Next"
     :file ,my/task-cockpit-file
     :todo "NEXT"
     :parent "Work Items")
    (:name "Suspended"
     :file ,my/task-cockpit-file
     :todo "HOLD"
     :parent "Work Items")
    (:name "Waiting"
     :file ,my/task-cockpit-file
     :todo "WAIT"
     :parent "Work Items")
    (:name "Planned"
     :file ,my/task-cockpit-file
     :todo "TODO"
     :parent "Work Items")
    (:name "Inbox"
     :file ,my/task-inbox-file
     :todo "TODO"
     :parent "Inbox"))
  "Cockpit Agendaで上下移動するワークフローレーン。")

(defun my/org-agenda-entry-marker ()
  "現在のAgenda行が参照するOrg見出しのマーカーを返す。"
  (or (org-get-at-bol 'org-hd-marker)
      (org-get-at-bol 'org-marker)
      (user-error "Agenda項目の行にカーソルを置いてください")))

(defun my/org-last-refile-marker ()
  "直前にRefileされたOrg見出しを指す新しいマーカーを返す。"
  (require 'bookmark)
  (let* ((bookmark-name
          (plist-get org-bookmark-names-plist
                     :last-refile))
         (bookmark
          (and bookmark-name
               (bookmark-get-bookmark
                bookmark-name
                'noerror)))
         (file
          (and bookmark
               (bookmark-get-filename bookmark)))
         (position
          (and bookmark
               (bookmark-get-position bookmark))))
    (unless (and file position)
      (user-error "Refile後の項目位置を取得できません"))
    (with-current-buffer (find-file-noselect file)
      (copy-marker position))))

(defun my/org-agenda-goto-marker (marker column)
  "Agenda内でMARKERに対応する項目へ移動し、COLUMNを復元する。"
  (goto-char (point-min))
  (let (found)
    (while (and (not found)
                (not (eobp)))
      (let ((line-marker
             (or (org-get-at-bol 'org-hd-marker)
                 (org-get-at-bol 'org-marker))))
        (if (and line-marker
                 (equal line-marker marker))
            (setq found t)
          (forward-line 1))))

    (unless found
      (user-error "移動後のAgenda項目を見つけられません"))

    (org-move-to-column column)
    (org-agenda-do-context-action)))

(defun my/org-agenda-lane-index (file todo)
  "FILEとTODOに対応するワークフローレーンの位置を返す。"
  (or
   (cl-position-if
    (lambda (lane)
      (and (file-equal-p file
                         (plist-get lane :file))
           (equal todo
                  (plist-get lane :todo))))
    my/org-agenda-workflow-lanes)
   (user-error
    "現在の項目はレーン移動の対象外です: %s / %s"
    file
    todo)))

(defun my/org-agenda-move-workflow-lane (offset)
  "Agenda項目とカーソルをOFFSET方向のワークフローレーンへ移動する。"
  (let* ((column (current-column))
         (marker (my/org-agenda-entry-marker))
         (source-file
          (org-with-point-at marker
            (buffer-file-name)))
         (source-todo
          (org-with-point-at marker
            (org-get-todo-state)))
         (source-index
          (my/org-agenda-lane-index
           source-file
           source-todo))
         (target-index (+ source-index offset)))

    (unless (< -1
               target-index
               (length my/org-agenda-workflow-lanes))
      (user-error
       "これ以上%sのレーンへは移動できません"
       (if (< offset 0) "上" "下")))

    (let* ((source-lane
            (nth source-index
                 my/org-agenda-workflow-lanes))
           (target-lane
            (nth target-index
                 my/org-agenda-workflow-lanes))
           (target-file
            (plist-get target-lane :file))
           (target-todo
            (plist-get target-lane :todo))
           moved-marker)

      (unwind-protect
          (progn
            (if (file-equal-p source-file target-file)
                ;; Cockpit内では、元見出しのTODO状態だけを変更する。
                (progn
                  (org-agenda-todo target-todo)
                  (setq moved-marker
                        (copy-marker
                         org-last-heading-marker)))

              ;; InboxとCockpitの境界では、見出し全体をファイル間移動する。
              (org-agenda-refile
               nil
               (my/org-refile-location
                target-file
                (plist-get target-lane :parent))
               t)
              (setq moved-marker
                    (my/org-last-refile-marker)))

            (my/save-task-org-buffers)
            (org-agenda-redo)
            (my/org-agenda-goto-marker
             moved-marker
             column)

            (message
             "%sから%sへ移動しました"
             (plist-get source-lane :name)
             (plist-get target-lane :name)))

        (when (markerp moved-marker)
          (set-marker moved-marker nil))))))

(defun my/org-agenda-move-workflow-up ()
  "Agenda項目を1つ上のワークフローレーンへ移動する。"
  (interactive)
  (my/org-agenda-move-workflow-lane -1))

(defun my/org-agenda-move-workflow-down ()
  "Agenda項目を1つ下のワークフローレーンへ移動する。"
  (interactive)
  (my/org-agenda-move-workflow-lane 1))

(use-package org-agenda
  :ensure nil
  :after org

  :bind
  (:map org-agenda-mode-map
        ("s-<up>" . my/org-agenda-move-workflow-up)
        ("s-<down>" . my/org-agenda-move-workflow-down))

  :config
  (setq org-agenda-custom-commands
        `(("c" "Cockpit"
           (
            ;; 現在作業中。
            (todo "DOING"
                  ((org-agenda-overriding-header "Active")
                   (org-agenda-files
                    '(,my/task-cockpit-file))))

            ;; 次に着手できる作業。
            (todo "NEXT"
                  ((org-agenda-overriding-header "Next")
                   (org-agenda-files
                    '(,my/task-cockpit-file))))

            ;; 中断中の作業。
            (todo "HOLD"
                  ((org-agenda-overriding-header "Suspended")
                   (org-agenda-files
                    '(,my/task-cockpit-file))))

            ;; 外部条件や回答待ち。
            (todo "WAIT"
                  ((org-agenda-overriding-header "Waiting")
                   (org-agenda-files
                    '(,my/task-cockpit-file))))

            ;; Cockpitへ登録済みだが、まだ着手順を決めていない作業。
            (todo "TODO"
                  ((org-agenda-overriding-header "Planned")
                   (org-agenda-files
                    '(,my/task-cockpit-file))))

            ;; 未整理の気づき。
            (todo "TODO"
                  ((org-agenda-overriding-header "Inbox")
                   (org-agenda-files
                    '(,my/task-inbox-file)))))))))
#+end_src
** 集中時間の管理

*** Org Pomodoro

Orgの作業項目に対してポモドーロタイマーを開始します。

- 作業時間: 25分
- 短い休憩: 5分
- 4回ごとの長い休憩: 15分

#+begin_src emacs-lisp
(use-package org-pomodoro
  :ensure t
  :commands org-pomodoro

  :custom
  ;; 1回の集中時間を25分にする。
  (org-pomodoro-length 25)

  ;; 通常の休憩時間を5分にする。
  (org-pomodoro-short-break-length 5)

  ;; 長い休憩時間を15分にする。
  (org-pomodoro-long-break-length 15)

  ;; 4回のポモドーロごとに長い休憩を入れる。
  (org-pomodoro-long-break-frequency 4))
#+end_src
** Org modeの表示

Orgファイルの見出しやテーブルを読みやすく表示するための設定です。

*** Org Modern

Org modeの見た目を整えます。

テーブルの表示はvalignに任せるため、
org-modernによるテーブル装飾は無効にします。

#+begin_src emacs-lisp
  (use-package org-modern
    :ensure t
    :after org

    :hook
    (org-mode . org-modern-mode)

    :custom
    (org-modern-table nil)

    ;; 見出しの先頭に折りたたみ状態を示す記号を表示する。
    (org-modern-star 'fold)

    ;; 折りたたみ中は「▸」、展開中は「▾」。
    ;; 1組だけ指定すると、すべての見出しレベルで共通して使用される。
    (org-modern-fold-stars
     '(("▸" . "▾")))

    ;; テーブルの縦線を1pxにする
    (org-modern-table-vertical 1)
  
    :config
    ;; Org Modernの記号にはNerd Fontを明示的に使用する。
    (set-face-attribute
     'org-modern-symbol nil
     :family "SauceCodePro Nerd Font Mono"))
#+end_src

* 補完と検索
** ミニバッファ補完
*** Vertico：候補の縦表示

Verticoは、ミニバッファの補完候補を縦方向に表示します。
#+begin_src emacs-lisp
(use-package vertico
  :ensure t

  :custom
  ;; 最大20件の候補を表示する。
  (vertico-count 20)

  ;; 候補数に応じて高さを伸縮する。
  (vertico-resize t)

  ;; ミニバッファの最大高さをフレームの30%にする。
  (max-mini-window-height 0.30)

  :init
  (vertico-mode))
#+end_src

*** Marginalia：候補の補足情報

ミニバッファの補完候補に説明を表示します。
#+begin_src emacs-lisp
(use-package marginalia
  :ensure t

  :init
  (marginalia-mode 1))
#+end_src
*** Nerd Icons Completion：補完候補のアイコン表示

Verticoなどのミニバッファ補完候補に、
ファイル、ディレクトリ、バッファ、コマンドなどに応じたアイコンを表示します。

#+begin_src emacs-lisp
  (use-package nerd-icons-completion
    :ensure t
    :after marginalia

    :config
    ;; ミニバッファの補完候補へNerd Iconsを表示する。
    (nerd-icons-completion-mode)

    ;; Marginaliaの有効・無効とアイコン表示を連動させる。
    (add-hook
     'marginalia-mode-hook
     #'nerd-icons-completion-marginalia-setup))
#+end_src
*** Orderless：柔軟な絞り込み

スペースで区切った複数の文字列を使って、
補完候補を柔軟に絞り込みます。
#+begin_src emacs-lisp
(use-package orderless
  :ensure t

  :custom
  (completion-styles '(orderless basic))

  ;; ファイル名では、ディレクトリの部分入力を使いやすくする。
  (completion-category-overrides
   '((file (styles partial-completion)))))
#+end_src

*** Consult：検索・移動コマンド

ミニバッファを利用した検索・移動機能を強化します。

#+begin_src emacs-lisp
  (use-package consult
    :ensure t
    :after bufferlo

    :custom
    ;; 現在のTab Barに属するバッファを既定の候補にする。
    (consult-buffer-list-function
     #'bufferlo-buffer-list)

    :bind
    (("s-r" . consult-buffer)

     ;; ブックマークをミニバッファで選択する。
     ("s-m" . consult-bookmark)

     ("s-f" . consult-line)
     ("M-g f" . consult-flymake)))
#+end_src
** バッファ補完
*** Corfu：バッファ内補完

Emacs標準の補完機構を利用して、コード編集中の候補をポップアップ表示します。
LSP Modeなどが提供する補完候補も同じ画面で扱います。

#+begin_src emacs-lisp
  (use-package corfu
    :ensure t

    :custom
    ;; 候補の末尾と先頭を循環できるようにする。
    (corfu-cycle t)

    ;; 入力中に候補を自動表示する。
    (corfu-auto t)

    ;; TRAMPや言語サーバー利用時に過剰な問い合わせを避ける。
    (corfu-auto-delay 0.25)
    (corfu-auto-prefix 2)

    ;; 候補を自動選択せず、明示的に選んだときだけ確定する。
    (corfu-preselect 'prompt)

    :init
    (global-corfu-mode 1)

    :config
    ;; RETによる候補確定を無効にし、改行時の誤確定を防ぐ。
    ;; 候補の補完・確定にはTABを使用する。
    (keymap-unset corfu-map "RET")

    ;; 選択中の候補について、利用可能な説明を表示する。
    (corfu-popupinfo-mode 1))
#+end_src

* 開発ツール

ターミナル、バージョン管理、コード補完、言語サーバーなど、
開発作業で利用するパッケージをまとめます。

** プロジェクトとリモート開発

*** Project

Emacs標準のプロジェクト管理機能を使用します。
Gitリポジトリ内に =cabal.project= がある場合は、
そのディレクトリを独立したプロジェクトルートとして認識します。

- =Cmd+p=: 現在のプロジェクト内のファイルを開く

#+begin_src emacs-lisp
  (use-package project
    :ensure nil

    :bind
    (("s-p" . project-find-file))

    :custom
    ;; Gitバックエンドを持たない分割プロジェクトでも、
    ;; Haskellのビルド生成物を候補から除外する。
    (project-vc-ignores
     '("dist-newstyle/"))

    :config
    (add-to-list
     'project-vc-extra-root-markers
     "cabal.project"))
#+end_src

*** TRAMP

TRAMP接続先の通常PATHに加えて、GHCupとユーザー単位の
実行ファイル置き場をリモートプログラムの検索対象にします。
特定のプロジェクト名、コンテナ名、接続先ユーザーには依存しません。

#+begin_src emacs-lisp
(use-package tramp
  :ensure nil

  :config
  ;; 接続先ユーザー自身のPATHを取り込む。
  (add-to-list
   'tramp-remote-path
   'tramp-own-remote-path)

  ;; GHCupで導入したGHC、Cabal、HLSなど。
  (add-to-list
   'tramp-remote-path
   "~/.ghcup/bin")

  ;; cabal installなどでユーザー単位に導入したコマンド。
  (add-to-list
   'tramp-remote-path
   "~/.local/bin"))
#+end_src

** 共通の開発支援

*** EditorConfig

プロジェクトの =.editorconfig= を読み取り、インデント幅や改行コードなどを
バッファへ反映します。特定のプロジェクト名やパスには依存しません。
#+begin_src emacs-lisp
(use-package editorconfig
  :ensure t

  :config
  (editorconfig-mode 1))
#+end_src

*** Rainbow Delimiters

プログラム用バッファで、括弧・角括弧・波括弧を
入れ子の深さに応じて色分けします。
配色はRetro Hacker Blueテーマ側で定義します。

#+begin_src emacs-lisp
(use-package rainbow-delimiters
  :ensure t

  :hook
  (prog-mode . rainbow-delimiters-mode))
#+end_src

*** Indent Bars

プログラム用バッファで、空白によるインデント位置へ細い縦線を表示します。
Tree-sitterのスコープ解析には依存せず、各メジャーモードとEditorConfigが決めた
インデント幅をそのまま表示へ使用します。

#+begin_src emacs-lisp
(use-package indent-bars
  :ensure t

  :hook
  (prog-mode . indent-bars-mode)

  :custom
  ;; Retro Hacker Blueの背景で主張しすぎない暗い青灰色。
  (indent-bars-color '("#252632" :blend 1.0))

  ;; すべての深さで同じ色を使う。
  (indent-bars-color-by-depth nil)

  ;; キャプチャのような細い実線を表示する。
  (indent-bars-width-frac 0.10)
  (indent-bars-pad-frac 0.10)
  (indent-bars-pattern ".")

  ;; 現在位置だけを強調せず、すべてのガイドを同じ濃さで表示する。
  (indent-bars-highlight-current-depth nil)

  ;; 空行にも前後のインデントを引き継いでガイドを表示する。
  (indent-bars-display-on-blank-lines t))
#+end_src

** ターミナル

*** Vterm

Emacs内で高速なターミナルを利用します。
#+begin_src emacs-lisp
  (use-package vterm
    :ensure t
    :commands vterm

    :hook
    (vterm-mode . (lambda ()
                    ;; ターミナルでは行番号を表示しない。
                    (display-line-numbers-mode -1)))

    :bind
    (:map vterm-mode-map
          ;; vterm内でもCmd+jでエディタへ戻る。
          ("s-j" . vterm-toggle)

          ;; 分割表示したvterm内でもCmd+Shift+jで元の画面へ戻る。
          ("s-J" . my/vterm-toggle-split))

    :custom
    ;; ローカルのmacOSではZshを使う。
    (vterm-shell "/bin/zsh")

    ;; TRAMP接続先では、接続方式に応じたシェルを使う。
    (vterm-tramp-shells
     '(("ssh" login-shell)
       ("scp" login-shell)
       ("docker" "/bin/bash")))

    :config
    ;; C-yをEmacsで処理せず、ターミナル側へ送る。
    (keymap-set vterm-mode-map "C-y" #'vterm--self-insert))
#+end_src
*** Vterm Toggle

Cmd+jでプロジェクト単位のvtermを全画面表示・非表示にします。
Cmd+Shift+jでは、現在のウィンドウ構成に応じて分割表示します。

- 1ウィンドウ、または上下分割: フレームの右50%
- 左右分割: フレームの下30%

#+begin_src emacs-lisp
  (use-package vterm-toggle
    :ensure t

    :preface
    ;; vterm-toggle本体が遅延読み込みされる前に、
    ;; この変数を動的束縛される変数として宣言しておく。
    (defvar vterm-toggle-fullscreen-p)

    (defun my/vterm-toggle--left-right-layout-p ()
      "選択ウィンドウに左右隣接するウィンドウがあるとき、非nilを返す。"
      (or (window-in-direction 'left)
          (window-in-direction 'right)))

    (defun my/vterm-toggle--display-buffer (buffer _alist)
      "現在のウィンドウ構成に応じたフレーム位置へBUFFERを表示する。"
      (if (my/vterm-toggle--left-right-layout-p)
          (display-buffer-in-direction
           buffer
           '((direction . below)
             (window . root)
             (window-height . 0.30)))
        (display-buffer-in-direction
         buffer
         '((direction . right)
           (window . root)
           (window-width . 0.50)))))

    (defun my/vterm-toggle-split ()
      "現在のウィンドウ構成に応じてvtermを分割表示・非表示にする。"
      (interactive)
      (let ((vterm-toggle-fullscreen-p nil)
            (display-buffer-alist
             (cons
              '((lambda (buffer-or-name _action)
                  (let ((buffer (get-buffer buffer-or-name)))
                    (and buffer
                         (with-current-buffer buffer
                           (derived-mode-p 'vterm-mode)))))
                (display-buffer-reuse-window
                 my/vterm-toggle--display-buffer))
              display-buffer-alist)))
        (vterm-toggle)))

    ;; vterm-toggleを遅延読み込み可能なコマンドとして登録する。
    :commands vterm-toggle

    ;; Emacs起動直後からCmd+jとCmd+Shift+jを使えるようにする。
    :bind
    (("s-j" . vterm-toggle)
     ("s-J" . my/vterm-toggle-split))

    :custom
    ;; 通常のvterm-toggleは全画面で表示する。
    (vterm-toggle-fullscreen-p t)

    ;; vtermを隠したとき、元のウィンドウ構成へ戻す。
    ;; configrationはパッケージ側の変数名どおりの綴り。
    (vterm-toggle-hide-method 'reset-window-configration)

    ;; プロジェクトごとにvtermを管理する。
    (vterm-toggle-scope 'project)

    ;; 新しいvtermをプロジェクトルートで起動する。
    (vterm-toggle-project-root t))
#+end_src

** バージョン管理

*** Magit

Emacs上でGitを操作します。
#+begin_src emacs-lisp
(use-package magit
  :ensure t
  :commands magit-status
  :bind
  ("C-x g" . magit-status))
#+end_src

*** Diff HL：変更行の表示

Gitで追加・変更・削除された行を、ウィンドウ左端のフリンジへ表示します。
保存前の変更も少し待つと自動的に反映されます。

| 表示 | 意味         |
|------+--------------|
| 緑   | 追加された行 |
| 黄   | 変更された行 |
| 赤   | 削除された行 |

#+begin_src emacs-lisp
(use-package diff-hl
  :ensure t

  :init
  ;; Gitなどのバージョン管理下にあるファイルで自動的に有効化する。
  (global-diff-hl-mode 1)

  :config
  ;; 保存を待たず、編集中の差分も自動的に更新する。
  (diff-hl-flydiff-mode 1)

  ;; Magitでステージ・コミットなどを行った後に表示を更新する。
  (with-eval-after-load 'magit
    (add-hook 'magit-post-refresh-hook
              #'diff-hl-magit-post-refresh)))
#+end_src

** Language Server Protocol

言語に依存しないLSPクライアント基盤を設定します。
各言語では、対応するLanguage Serverとメジャーモードを追加し、
その言語のフックから =lsp-deferred= を呼び出します。

*** LSP Mode：共通LSP基盤

補完、診断、定義参照、リネーム、コードアクション、整形などを、
Language Server Protocol経由で各言語へ提供します。

- =C-c l a=: コードアクションを実行
- =C-c l r=: プロジェクト全体で識別子を変更
- =C-c l f=: 現在のバッファを整形
- =M-g s=: ワークスペースシンボルを検索
- =C-c L=: LSP Modeのコマンドプレフィックス

#+begin_src emacs-lisp
  (defun my/lsp-format-buffer-before-save ()
    "LSP管理中のバッファを保存前に整形する。"
    (when (bound-and-true-p lsp-mode)
      (lsp-format-buffer)))

  (use-package lsp-mode
    :ensure t
    :demand t

    :init
    ;; lsp-mode標準のs-lは、既存の行選択キーと競合するため使用しない。
    (setq lsp-keymap-prefix "C-c L")

    :custom
    ;; Corfuがcompletion-at-point-functionsを直接利用するため、
    ;; Companyの自動設定は行わない。
    (lsp-completion-provider :none)

    ;; LSPのCAPF連携自体は有効にする。
    (lsp-completion-enable t)

    ;; import追加など、補完候補に付随する編集も適用する。
    (lsp-completion-enable-additional-text-edit t)

    ;; 現在Yasnippetを導入していないため、snippet展開は無効にする。
    (lsp-enable-snippet nil)

    ;; 現在の診断UIであるFlymakeを維持する。
    (lsp-diagnostics-provider :flymake)

    ;; 最後の管理対象バッファを閉じたとき、Language Serverも終了する。
    (lsp-keep-workspace-alive nil)

    ;; TRAMP先では、接続先環境のLanguage Serverを使用する。
    (lsp-auto-register-remote-clients t)

    ;; 画面上部に、現在のプロジェクト・ファイル・シンボル階層を表示する。
    ;; VS CodeのBreadcrumbに近い表示。
    (lsp-headerline-breadcrumb-enable t)

    ;; 関数や定義の上に、Language Serverが提供する補助情報を表示する。
    ;; HLSでは型情報や参照情報などが表示されることがある。
    ;; 画面が賑やかすぎる場合は、ここだけnilへ戻す。
    (lsp-lens-enable t)

    ;; 現在位置で利用可能なCode Actionがあることを
    ;; モードラインへ表示する。
    (lsp-modeline-code-actions-enable t)

    ;; エラー・警告・情報の件数をモードラインへ表示する。
    (lsp-modeline-diagnostics-enable t)

    ;; Language Serverの接続状態や処理状態を
    ;; モードラインへ表示する。
    (lsp-modeline-workspace-status-enable t)
    
    ;; 通常利用ではLSP通信ログを保存しない。
    (lsp-log-io nil)

    :config
    ;; Breadcrumbの外周と各セグメントから枠線を完全に取り除く。
    ;; テーマ側でもheader-lineのboxを無効化しているが、
    ;; テーマ再読込やFace継承後も確実に維持するため、ここでも指定する。
    (dolist (face '(header-line header-line-highlight))
      (set-face-attribute
       face nil
       :box nil
       :overline nil
       :underline nil))

    (with-eval-after-load 'lsp-headerline
      (dolist (face
               '(lsp-headerline-breadcrumb-separator-face
                 lsp-headerline-breadcrumb-path-face
                 lsp-headerline-breadcrumb-path-error-face
                 lsp-headerline-breadcrumb-path-warning-face
                 lsp-headerline-breadcrumb-path-info-face
                 lsp-headerline-breadcrumb-path-hint-face
                 lsp-headerline-breadcrumb-project-prefix-face
                 lsp-headerline-breadcrumb-unknown-project-prefix-face
                 lsp-headerline-breadcrumb-symbols-face
                 lsp-headerline-breadcrumb-symbols-error-face
                 lsp-headerline-breadcrumb-symbols-warning-face
                 lsp-headerline-breadcrumb-symbols-info-face
                 lsp-headerline-breadcrumb-symbols-hint-face
                 lsp-headerline-breadcrumb-deprecated-face))
        ;; 診断を示す波線は残し、ボックスと上線だけを消す。
        (set-face-attribute face nil :box nil :overline nil)))

    :bind
    (:map lsp-mode-map
          ("C-c l a" . lsp-execute-code-action)
          ("C-c l r" . lsp-rename)
          ("C-c l f" . lsp-format-buffer)
          ("M-g s" . lsp-workspace-symbol)))
#+end_src

*** LSP UI：高機能UI

=lsp-ui= が提供する高水準UIを有効にします。
パッケージには =lsp-ui-doc=、=lsp-ui-imenu=、=lsp-ui-peek=、
=lsp-ui-sideline= が含まれますが、ここではドキュメント表示と
シンボル一覧を中心に使用します。

- =C-c l h=: カーソル位置の型・ドキュメントを表示
- =C-c l i=: 現在のバッファのシンボル一覧を右側へ表示
- =C-c l p d=: 定義をPeek表示
- =C-c l p r=: 参照をPeek表示
- =C-c l p i=: 実装をPeek表示

#+begin_src emacs-lisp
  (defun my/lsp-ui-doc-style-frame (frame _window)
    "LSP UI DocのFRAMEをフラットなダークカードとして整える。"
    ;; Child Frame内だけに適用し、通常バッファのテーマは変更しない。
    (set-face-attribute
     'default frame
     :background "#0B1324"
     :foreground "#D8E8FF")

    (set-face-attribute
     'fringe frame
     :background "#0B1324"
     :foreground "#506A91")

    (set-face-attribute
     'header-line frame
     :background "#15213A"
     :foreground "#EAF2FF"
     :weight 'semi-bold
     :box nil
     :overline nil
     :underline nil)

    ;; 外枠と内側余白をカード本体と同色にして、白い輪郭をなくす。
    (set-face-attribute
     'internal-border frame
     :background "#0B1324")

    (when (facep 'child-frame-border)
      (set-face-attribute
       'child-frame-border frame
       :background "#0B1324"))

    ;; コード部分だけをわずかに明るい面へ置き、本文と区別する。
    (when (facep 'markdown-code-face)
      (set-face-attribute
       'markdown-code-face frame
       :background "#111C32"
       :foreground "#BFD7FF"
       :extend t))

    (when (facep 'markdown-inline-code-face)
      (set-face-attribute
       'markdown-inline-code-face frame
       :background "#111C32"
       :foreground "#87CEEB")))

  (require 'cl-lib)

  (defun my/lsp-ui-peek-read-file-with-coding
      (original-function file)
    "LSP UI PeekがFILEを文字コード判定付きで読み込むようにする。"
    ;; lsp-ui-peekは未訪問ファイルを
    ;; insert-file-contents-literallyで読み込むため、
    ;; UTF-8の日本語が\345のような8進表記になることがある。
    ;; Peekの読み込み中だけ通常の文字コード判定を使う。
    (cl-letf
        (((symbol-function 'insert-file-contents-literally)
          #'insert-file-contents))
      (funcall original-function file)))

  (defvar-keymap my/lsp-ui-peek-prefix-map
    :doc "LSP UI Peekを開くためのプレフィックスマップ。"
    "d" #'lsp-ui-peek-find-definitions
    "r" #'lsp-ui-peek-find-references
    "i" #'lsp-ui-peek-find-implementation)

  (defun my/lsp-ui-imenu-setup ()
    "LSP UI Imenuバッファの表示を整える。"
    ;; グローバル行番号表示の対象から外す。
    (display-line-numbers-mode -1))

  (defun my/lsp-ui-imenu-use-thin-bars
      (original-function bars index depth for-title is-last)
    "LSP UI Imenuの太い罫線文字を細い罫線文字へ置き換える。"
    (pcase
        (funcall original-function
                 bars
                 index
                 depth
                 for-title
                 is-last)
      (" ┃ " " │ ")
      (" ┣ " " ├ ")
      (" ┗ " " └ ")
      (bar bar)))

  ;; nilへ変更すれば、見出しアイコンを残して葉アイコンだけ非表示にできる。
  (defcustom my/lsp-ui-imenu-show-leaf-icons t
    "LSP UI Imenuの葉項目へNerd Fontアイコンを表示する。"
    :type 'boolean
    :group 'lsp-ui-imenu)

  (defconst my/lsp-ui-imenu-title-icon-rules
    '(("\\_<\\(?:imports?\\|modules?\\|namespaces?\\|packages?\\)\\_>"
       . "nf-cod-symbol_namespace")
      ("\\_<class\\(?:es\\)?\\_>"
       . "nf-cod-symbol_class")
      ("\\_<interfaces?\\_>"
       . "nf-cod-symbol_interface")
      ("\\_<\\(?:structs?\\|structures?\\|records?\\|types?\\|data\\)\\_>"
       . "nf-cod-symbol_structure")
      ("\\_<enums?\\_>"
       . "nf-cod-symbol_enum")
      ("\\_<events?\\_>"
       . "nf-cod-symbol_event")
      ("\\_<\\(?:methods?\\|functions?\\|procedures?\\|constructors?\\)\\_>"
       . "nf-cod-symbol_method")
      ("\\_<\\(?:fields?\\|propert\\(?:y\\|ies\\)\\)\\_>"
       . "nf-cod-symbol_field")
      ("\\_<constants?\\_>"
       . "nf-cod-symbol_constant")
      ("\\_<variables?\\_>"
       . "nf-cod-symbol_variable")
      ("\\_<files?\\_>"
       . "nf-cod-symbol_file"))
    "LSP UI Imenuの見出しとNerd Fontアイコンの対応。")

  (defun my/lsp-ui-imenu-matching-icon-name (title)
    "TITLEに対応するNerd Fontアイコン名を返す。"
    (let ((case-fold-search t)
          (plain-title
           (substring-no-properties (or title ""))))
      (cl-loop
       for (regexp . icon)
       in my/lsp-ui-imenu-title-icon-rules
       when (string-match-p regexp plain-title)
       return icon)))

  (defun my/lsp-ui-imenu-insert-icon-before-title
      (rendered-title title icon)
    "RENDERED-TITLE内のTITLE直前へICONを挿入する。"
    (let* ((plain-rendered-title
            (substring-no-properties rendered-title))
           (plain-title
            (substring-no-properties title))
           (position
            (string-match
             (regexp-quote plain-title)
             plain-rendered-title)))
      (if position
          (concat
           (substring rendered-title 0 position)
           icon
           " "
           (substring rendered-title position))
        rendered-title)))

  (defun my/lsp-ui-imenu-add-icon-to-title
      (original-function title &rest arguments)
    "LSP UI ImenuのTITLEへNerd Fontアイコンを付ける。"
    (let ((overlay-put-function
           (symbol-function 'overlay-put))
          (icon
           (nerd-icons-codicon
            (or
             (my/lsp-ui-imenu-matching-icon-name title)
             "nf-cod-symbol_structure"))))
      (cl-letf
          (((symbol-function 'overlay-put)
            (lambda (overlay property value)
              (funcall
               overlay-put-function
               overlay
               property
               (if (and (eq property 'after-string)
                        (stringp value))
                   (my/lsp-ui-imenu-insert-icon-before-title
                    value
                    title
                    icon)
                 value)))))
        (apply original-function title arguments))))

  (defun my/lsp-ui-imenu-add-icon-to-leaf
      (original-function
       title index entry padding bars depth color-index is-last)
    "LSP UI Imenuの葉項目へNerd Fontアイコンを付ける。"
    (let ((line
           (funcall
            original-function
            title
            index
            entry
            padding
            bars
            depth
            color-index
            is-last)))
      (if (not my/lsp-ui-imenu-show-leaf-icons)
          line
        (let* ((name-position
                (min (+ padding (* depth 3))
                     (length line)))
               (icon
                (concat
                 (nerd-icons-codicon
                  (or
                   (my/lsp-ui-imenu-matching-icon-name title)
                   "nf-cod-symbol_method"))
                 " ")))
          ;; アイコン上でも移動・選択できるよう、葉項目の情報を引き継ぐ。
          (add-text-properties
           0
           (length icon)
           `(index ,index
                   title ,title
                   marker ,(cdr entry)
                   padding ,padding
                   depth ,depth)
           icon)
          (concat
           (substring line 0 name-position)
           icon
           (substring line name-position))))))

  (use-package lsp-ui
    :ensure t
    :after (lsp-mode nerd-icons)

    :hook
    ((lsp-mode . lsp-ui-mode)
     (lsp-ui-imenu-mode . my/lsp-ui-imenu-setup))

    :bind
    (:map lsp-ui-mode-map
          ("C-c l h" . lsp-ui-doc-glance)
          ("C-c l i" . lsp-ui-imenu))

    :custom
    ;; カーソル位置の型・シグネチャ・ドキュメントを
    ;; Child Frameへ表示する。
    (lsp-ui-doc-enable t)
    (lsp-ui-doc-use-childframe t)

    ;; カーソル移動は自動表示しない。
    ;; 詳細を確認したいときだけC-c l hで明示的に表示する。
    (lsp-ui-doc-show-with-cursor nil)
    ;; マウスを重ねたときは自動表示する。
    (lsp-ui-doc-show-with-mouse t)

    (lsp-ui-doc-position 'at-point)
    (lsp-ui-doc-delay 0.30)

    ;; 鮮やかなタイトルバーではなく、控えめなダークヘッダーを使用する。
    (lsp-ui-doc-header t)
    (lsp-ui-doc-include-signature t)
    (lsp-ui-doc-enhanced-markdown t)

    ;; VS CodeのHoverに近い、読みやすいカードサイズに抑える。
    (lsp-ui-doc-max-width 96)
    (lsp-ui-doc-max-height 18)

    ;; 枠色を本体背景と同じにし、内部余白として利用する。
    (lsp-ui-doc-border "#0B1324")

    ;; 現在のバッファにある型、関数、クラスなどを
    ;; VS CodeのOutlineに近い右側の一覧で表示する。
    (lsp-ui-imenu-enable t)
    (lsp-ui-imenu-buffer-position 'right)
    (lsp-ui-imenu-kind-position 'top)
    (lsp-ui-imenu-window-width 38)
    (lsp-ui-imenu-window-fix-width t)
    (lsp-ui-imenu-auto-refresh 'after-save)

    ;; 定義・参照・実装を、現在のウィンドウ内へ一時表示する。
    ;; M-.とM-?は標準Xrefとして残し、PeekはC-c l p配下で使い分ける。
    (lsp-ui-peek-enable t)

    ;; 結果が1件だけでも直接ジャンプせず、必ずPeekを表示する。
    (lsp-ui-peek-always-show t)

    ;; ファイル名だけを表示し、一覧を簡潔に保つ。
    (lsp-ui-peek-show-directory nil)

    ;; コードと候補一覧を見渡せる、ややコンパクトなサイズにする。
    (lsp-ui-peek-peek-height 16)
    (lsp-ui-peek-list-width 42)
    (lsp-ui-peek-fontify 'on-demand)

    ;; Sidelineは右端揃えの表示位置を設定で変更できないため使用しない。
    ;; 診断はFlymake、詳細情報はLSP UI Docへ一本化する。
    (lsp-ui-sideline-enable nil)

    :config
    (with-eval-after-load 'lsp-ui-imenu
      ;; Imenuのツリーを、太い罫線から細い罫線へ変更する。
      (unless
          (advice-member-p
           #'my/lsp-ui-imenu-use-thin-bars
           #'lsp-ui-imenu--get-bar)
        (advice-add
         #'lsp-ui-imenu--get-bar
         :around
         #'my/lsp-ui-imenu-use-thin-bars))

      ;; グループ見出しや親シンボルへNerd Fontアイコンを付ける。
      (dolist
          (function
           '(lsp-ui-imenu--put-toplevel-title
             lsp-ui-imenu--put-subtitle))
        (unless
            (advice-member-p
             #'my/lsp-ui-imenu-add-icon-to-title
             function)
          (advice-add
           function
           :around
           #'my/lsp-ui-imenu-add-icon-to-title)))

      ;; 葉項目へNerd Fontアイコンを付ける。
      (unless
          (advice-member-p
           #'my/lsp-ui-imenu-add-icon-to-leaf
           #'lsp-ui-imenu--make-line)
        (advice-add
         #'lsp-ui-imenu--make-line
         :around
         #'my/lsp-ui-imenu-add-icon-to-leaf)))

    ;; Child Frameの背景とヘッダーを、白枠のないフラットなカードにする。
    (set-face-attribute
     'lsp-ui-doc-background nil
     :background "#0B1324")

    (set-face-attribute
     'lsp-ui-doc-header nil
     :background "#15213A"
     :foreground "#EAF2FF"
     :weight 'semi-bold
     :box nil
     :overline nil
     :underline nil)

    (set-face-attribute
     'lsp-ui-doc-highlight-hover nil
     :inherit 'highlight
     :box nil)

    (set-face-attribute
     'lsp-ui-doc-url nil
     :inherit 'link
     :underline t)

    ;; 本体背景と同色の8px領域を設け、カード内に自然な余白を作る。
    (setf (alist-get 'internal-border-width
                     lsp-ui-doc-frame-parameters)
          8)

    (add-hook
     'lsp-ui-doc-frame-hook
     #'my/lsp-ui-doc-style-frame)

    ;; 定義・参照・実装のPeekコマンドをC-c l p配下へまとめる。
    (keymap-set
     lsp-ui-mode-map
     "C-c l p"
     my/lsp-ui-peek-prefix-map)

    ;; 未訪問ファイルも通常の文字コード判定で読み込み、
    ;; 日本語コメントが8進エスケープ表示になる問題を避ける。
    (unless
        (advice-member-p
         #'my/lsp-ui-peek-read-file-with-coding
         #'lsp-ui-peek--get-xrefs-in-file)
      (advice-add
       #'lsp-ui-peek--get-xrefs-in-file
       :around
       #'my/lsp-ui-peek-read-file-with-coding)))
#+end_src

** Haskell開発

Haskellの編集、HLSとの接続、保存時整形、シンボル検索を設定します。
設定はHaskell全般を対象とし、特定のリポジトリ名やディレクトリには依存しません。

*** Haskell TS Mode

通常のHaskellソース（=.hs=）では、Tree-sitterを利用した
=haskell-ts-mode= を既定のメジャーモードとして使用します。

構文ハイライトは最高レベルを使用します。
インデント機能は安定性を優先して無効にし、保存時のFourmolu整形へ任せます。

#+begin_src emacs-lisp
(use-package haskell-ts-mode
  :ensure t

  :mode
  (("\\.hs\\'" . haskell-ts-mode))

  :custom
  ;; Tree-sitterによる構文ハイライトを最高レベルで使用する。
  (haskell-ts-font-lock-level 4)

  ;; haskell-ts-modeのインデントは使用せず、Fourmoluへ任せる。
  (haskell-ts-use-indent nil)

  :config
  ;; MacホストへインストールするHaskell grammarの取得元。
  (add-to-list
   'treesit-language-source-alist
   '(haskell
     "https://github.com/tree-sitter/tree-sitter-haskell"
     "v0.23.1")))
#+end_src

*** Haskell Mode

Literate HaskellソースとCabalファイルのメジャーモードを提供します。
LSP ModeのXrefバックエンドを使用するため、Haskell Mode独自の定義ジャンプより
Emacs標準のXref操作を優先します。

| キー | 操作 |
|------+------|
| =M-.= | カーソル位置の識別子の定義へ移動 |
| =M-?= | カーソル位置の識別子の参照箇所を表示 |

#+begin_src emacs-lisp
  (use-package haskell-mode
    :ensure t

    :mode
    (("\\.lhs\\'" . literate-haskell-mode)
     ("\\.cabal\\'" . haskell-cabal-mode))

    :config
    ;; Haskell ModeとInteractive Haskell Modeの独自ジャンプを、
    ;; LSP Modeと連携するEmacs標準Xrefへ統一する。
    (with-eval-after-load 'haskell
      (keymap-set
       haskell-mode-map
       "M-."
       #'xref-find-definitions)
      (keymap-set
       haskell-mode-map
       "M-?"
       #'xref-find-references)
      (keymap-set
       interactive-haskell-mode-map
       "M-."
       #'xref-find-definitions)
      (keymap-set
       interactive-haskell-mode-map
       "M-?"
       #'xref-find-references)))
    #+end_src

*** LSP Haskell：Haskell固有設定

共通のLSP Mode基盤へHaskell Language Serverを接続します。
ローカル環境でもTRAMP接続先でも、各環境のPATHにある
=haskell-language-server-wrapper= を使用します。

Haskellバッファでは保存時にFourmoluで整形し、
スコープ外の識別子を補完したときはimportを自動追加します。

#+begin_src emacs-lisp
(defcustom my/lsp-haskell-cabal-formatting-provider
  "cabal-gild"
  "HLSがCabalファイルの整形に使用するformatter。"
  :type 'string
  :group 'haskell)

(defun my/haskell-lsp-setup ()
  "HaskellバッファでLSP Modeと保存時整形を有効にする。"
  (lsp-deferred)
  (add-hook
   'before-save-hook
   #'my/lsp-format-buffer-before-save
   nil
   t))

(use-package lsp-haskell
  :ensure t
  :after lsp-mode

  :hook
  ((haskell-mode . my/haskell-lsp-setup)
   (haskell-ts-mode . my/haskell-lsp-setup))

  :custom
  ;; HLS wrapperをPATHから解決する。
  (lsp-haskell-server-path "haskell-language-server-wrapper")

  ;; HaskellソースのformatterとしてFourmoluを使用する。
  (lsp-haskell-formatting-provider "fourmolu")

  ;; スコープ外の識別子を補完したとき、importを自動追加する。
  (lsp-haskell-plugin-ghcide-completions-config-auto-extend-on t)

  :config
  ;; lsp-haskellが専用変数を提供していないCabal formatterを明示する。
  (lsp-register-custom-settings
   '(("haskell.cabalFormattingProvider"
      my/lsp-haskell-cabal-formatting-provider))))
#+end_src

** Emacs Lisp開発
*** Flymake：Emacs Lispのリアルタイム診断

Emacs LispのバッファでFlymakeを有効にし、
編集中のエラーや警告をリアルタイムで表示します。

#+begin_src emacs-lisp
  (use-package flymake
    :ensure nil

    :hook
    (emacs-lisp-mode . flymake-mode))
#+end_src
* 外観

Emacs全体のフォント、テーマ、配色に関する設定をまとめます。

** フォント

GUI版Emacsで使用する等幅フォントを設定します。

#+begin_src emacs-lisp
  (defun my/apply-default-font (&optional frame)
    "FRAMEへ通常バッファ用の標準フォントを設定する。"
    (with-selected-frame (or frame (selected-frame))
      (when (display-graphic-p)
        (set-face-attribute
         'default
         (selected-frame)
;         :family "SauceCodePro Nerd Font Mono"
         :family "UDEV Gothic NF"
         :height 140))))

  (defun my/apply-org-font ()
    "現在のOrgバッファへUDEV Gothic NFを適用する。"
    ;; Org本文や通常テキスト。
    (face-remap-add-relative
     'default
     :family "UDEV Gothic NF")

    ;; Orgテーブル、コード、verbatimなどで使われることがある。
    (face-remap-add-relative
     'fixed-pitch
     :family "UDEV Gothic NF")

    ;; 見出しなどがvariable-pitchを継承していてもUDEVへ統一する。
    (face-remap-add-relative
     'variable-pitch
     :family "UDEV Gothic NF"))

  ;; 標準行高の25%を行間として追加する。
  (setq-default line-spacing 0.25)

  ;; 現在のフレームへ通常フォントを適用する。
  (my/apply-default-font)

  ;; 後から作られたフレームにも通常フォントを適用する。
  (add-hook
   'after-make-frame-functions
   #'my/apply-default-font)

  ;; OrgバッファだけUDEV Gothic NFへ変更する。
  (add-hook
   'org-mode-hook
   #'my/apply-org-font)
#+end_src

** Retro Hacker Blueテーマ

VS CodeのRetro Hacker Blueを元にしたカラーテーマを使用します。
#+begin_src emacs-lisp
(add-to-list
 'custom-theme-load-path
 (expand-file-name "themes/" user-emacs-directory))

;; 複数テーマが重なって有効になるのを防ぐ。
(mapc #'disable-theme custom-enabled-themes)

(load-theme 'retro-hacker-blue t)
#+end_src
* 編集操作とキーバインド

独自の編集コマンドと、Emacs全体で使用するキー割り当てをまとめます。
** Better Jumper：ナビゲーション履歴の戻る・進む

ファイルバッファ内の明示的なジャンプと、同じウィンドウでの
ファイルバッファ切り替えを、共通のナビゲーション履歴へ記録します。
通常のカーソル移動や、Diredなどの非ファイルバッファは記録しません。

異なるウィンドウへ新しいファイルを開いた場合は、
移動元のウィンドウとカーソル位置も履歴として保持します。
戻る・進むでは同じファイルを現在のウィンドウへ開き直さず、
元のウィンドウそのものを選択します。

- =<mouse-4>=: 1つ前の場所へ戻る
- =<mouse-5>=: 戻る前の場所へ進む

初期状態では、定義ジャンプ、Consultによるファイル内検索、
行番号指定のジャンプを明示的なジャンプとして扱います。
追跡対象と除外対象は、利用しながらコマンド単位で調整できます。

#+begin_src emacs-lisp
  (defcustom my/navigation-history-jump-commands
    '(xref-find-definitions
      consult-line
      consult-goto-line
      goto-line)
    "同一ファイル内でもナビゲーション履歴へ記録するジャンプコマンド。"
    :type '(repeat function)
    :group 'convenience)

  (defcustom my/navigation-history-ignored-buffer-switch-commands
    nil
    "ファイルバッファを切り替えても履歴へ記録しないコマンド。"
    :type '(repeat function)
    :group 'convenience)

  (defconst my/navigation-history--back-location-parameter
    'my/navigation-history-back-location
    "ウィンドウをまたいで戻る位置を保持するウィンドウパラメーター。")

  (defconst my/navigation-history--forward-location-parameter
    'my/navigation-history-forward-location
    "ウィンドウをまたいで進む位置を保持するウィンドウパラメーター。")

  (defvar my/navigation-history--before-location nil
    "現在のコマンドを実行する直前のファイル位置。")

  (defvar my/navigation-history--explicit-jump-p nil
    "現在のコマンドを明示的なジャンプとして扱うとき非nil。")

  (defvar my/navigation-history--skip-current-command-p nil
    "現在のコマンドによる移動を履歴へ記録しないとき非nil。")

  (defun my/navigation-history--file-buffer-p (&optional buffer)
    "BUFFERが通常のファイルを訪問しているとき非nilを返す。"
    (with-current-buffer (or buffer (current-buffer))
      (and buffer-file-name
           (not (minibufferp)))))

  (defun my/navigation-history--location-at-window
      (window &optional marker)
    "WINDOWのファイル位置を返す。
MARKERが有効なら、WINDOWの現在位置ではなくMARKERを使用する。"
    (when (window-live-p window)
      (let* ((buffer
              (if (and (markerp marker)
                       (marker-buffer marker))
                  (marker-buffer marker)
                (window-buffer window)))
             (position
              (if (and (markerp marker)
                       (marker-position marker))
                  (marker-position marker)
                (window-point window))))
        (when (and position
                   (buffer-live-p buffer)
                   (my/navigation-history--file-buffer-p buffer))
          (with-current-buffer buffer
            (list :buffer buffer
                  :marker (copy-marker position)
                  :window window))))))

  (defun my/navigation-history--capture-location ()
    "現在のファイル位置をナビゲーション履歴用に保存する。"
    (when (and (my/navigation-history--file-buffer-p)
               (bound-and-true-p better-jumper-local-mode))
      (my/navigation-history--location-at-window
       (selected-window))))

  (defun my/navigation-history--release-location (location)
    "LOCATIONが保持しているマーカーを解放する。"
    (let ((marker (plist-get location :marker)))
      (when (markerp marker)
        (set-marker marker nil))))

  (defun my/navigation-history--set-window-location
      (window parameter location)
    "WINDOWのPARAMETERへLOCATIONを設定する。
以前の位置があれば、そのマーカーを先に解放する。"
    (when (window-live-p window)
      (my/navigation-history--release-location
       (window-parameter window parameter))
      (set-window-parameter window parameter location)))

  (defun my/navigation-history--take-window-location
      (window parameter)
    "WINDOWのPARAMETERから位置を取り出し、PARAMETERを空にする。"
    (when (window-live-p window)
      (prog1 (window-parameter window parameter)
        (set-window-parameter window parameter nil))))

  (defun my/navigation-history--clear-forward-location (&optional window)
    "WINDOWに残っているウィンドウ間の進む履歴を破棄する。"
    (my/navigation-history--set-window-location
     (or window (selected-window))
     my/navigation-history--forward-location-parameter
     nil))

  (defun my/navigation-history--clear-command-state ()
    "コマンド間で保持しているナビゲーション判定用の状態を破棄する。"
    (my/navigation-history--release-location
     my/navigation-history--before-location)
    (setq my/navigation-history--before-location nil
          my/navigation-history--explicit-jump-p nil
          my/navigation-history--skip-current-command-p nil))

  (defun my/navigation-history--history-command-p (command)
    "COMMANDがナビゲーション履歴自体を移動するコマンドなら非nil。"
    (memq command
          '(my/navigation-history-backward
            my/navigation-history-forward
            better-jumper-jump-backward
            better-jumper-jump-forward
            better-jumper-jump-newest)))

  (defun my/navigation-history--record-location (location)
    "LOCATIONを現在のウィンドウのBetter Jumper履歴へ追加する。"
    (let ((buffer (plist-get location :buffer))
          (marker (plist-get location :marker)))
      (when (and (buffer-live-p buffer)
                 (markerp marker)
                 (marker-position marker))
        ;; 戻ったあとに新しいナビゲーションを行った場合は、
        ;; ウィンドウをまたぐ進む履歴もブラウザと同様に破棄する。
        (my/navigation-history--clear-forward-location)
        (with-current-buffer buffer
          (when (bound-and-true-p better-jumper-local-mode)
            (save-excursion
              (goto-char marker)
              (better-jumper-set-jump)))))))

  (defun my/navigation-history--before-command ()
    "コマンド実行前のファイル位置と移動種別を保存する。"
    ;; ミニバッファ内の再帰的なコマンドでは、
    ;; 呼び出し元で保存した位置を上書きしない。
    (unless (minibufferp)
      (my/navigation-history--clear-command-state)
      (setq my/navigation-history--before-location
            (my/navigation-history--capture-location)
            my/navigation-history--explicit-jump-p
            (memq this-command
                  my/navigation-history-jump-commands)
            my/navigation-history--skip-current-command-p
            (my/navigation-history--history-command-p
             this-command))))

  (defun my/navigation-history--location-changed-p (location)
    "LOCATIONから現在位置へ移動しているとき非nilを返す。"
    (let ((buffer (plist-get location :buffer))
          (marker (plist-get location :marker)))
      (or (not (eq buffer (current-buffer)))
          (and (eq buffer (current-buffer))
               (markerp marker)
               (marker-position marker)
               (/= (marker-position marker)
                   (point))))))

  (defun my/navigation-history--after-command ()
    "必要なナビゲーションだけ、実行前の位置を履歴へ追加する。"
    ;; ミニバッファのコマンド終了時は状態を維持し、
    ;; 元のコマンドが完了した時点で最終的な移動先を判定する。
    (unless (minibufferp)
      (let* ((location my/navigation-history--before-location)
             (source-buffer
              (and location
                   (plist-get location :buffer)))
             (source-window
              (and location
                   (plist-get location :window)))
             (buffer-switched-p
              (and source-buffer
                   (not (eq source-buffer
                            (current-buffer)))))
             (ignored-buffer-switch-p
              (memq this-command
                    my/navigation-history-ignored-buffer-switch-commands)))
        (unwind-protect
            (when (and location
                       (not my/navigation-history--skip-current-command-p)
                       (eq source-window (selected-window))
                       (my/navigation-history--file-buffer-p)
                       (bound-and-true-p better-jumper-local-mode)
                       (my/navigation-history--location-changed-p
                        location)
                       (or my/navigation-history--explicit-jump-p
                           (and buffer-switched-p
                                (not ignored-buffer-switch-p))))
              (my/navigation-history--record-location location))
          (my/navigation-history--clear-command-state)))))

  (defun my/navigation-history--position-state ()
    "現在のウィンドウ、バッファ、カーソル位置を返す。"
    (list (selected-window)
          (current-buffer)
          (point)))

  (defun my/navigation-history--position-state-changed-p (state)
    "現在位置がSTATEから変わっているとき非nilを返す。"
    (not (equal state
                (my/navigation-history--position-state))))

  (defun my/navigation-history--visit-location (location)
    "LOCATIONが指すウィンドウとファイル位置へ移動する。"
    (let ((window (plist-get location :window))
          (buffer (plist-get location :buffer))
          (marker (plist-get location :marker)))
      (when (and (window-live-p window)
                 (buffer-live-p buffer)
                 (markerp marker)
                 (marker-position marker))
        (select-window window)
        (unless (eq (window-buffer window) buffer)
          (set-window-buffer window buffer))
        (goto-char marker)
        t)))

  (defun my/navigation-history--follow-window-location
      (source-parameter target-parameter)
    "SOURCE-PARAMETERの位置へ移動し、戻り先をTARGET-PARAMETERへ保存する。"
    (let* ((source-window (selected-window))
           (target-location
            (my/navigation-history--take-window-location
             source-window
             source-parameter))
           (return-location
            (my/navigation-history--location-at-window
             source-window)))
      (cond
       ((and target-location
             return-location
             (my/navigation-history--visit-location
              target-location))
        ;; 移動後に選択されたウィンドウへ、逆方向の移動先を保存する。
        (my/navigation-history--set-window-location
         (selected-window)
         target-parameter
         return-location)
        (my/navigation-history--release-location
         target-location)
        t)

       (t
        (my/navigation-history--release-location
         target-location)
        (my/navigation-history--release-location
         return-location)
        nil))))

  (defun my/navigation-history--follow-back-location ()
    "ウィンドウをまたぐ直前の位置へ戻る。"
    (my/navigation-history--follow-window-location
     my/navigation-history--back-location-parameter
     my/navigation-history--forward-location-parameter))

  (defun my/navigation-history--follow-forward-location ()
    "ウィンドウをまたいで戻る前の位置へ進む。"
    (my/navigation-history--follow-window-location
     my/navigation-history--forward-location-parameter
     my/navigation-history--back-location-parameter))

  (defun my/navigation-history-backward ()
    "同一ウィンドウの履歴を戻り、なければ移動元ウィンドウへ戻る。"
    (interactive)
    (let ((before (my/navigation-history--position-state)))
      (better-jumper-jump-backward)
      (unless (my/navigation-history--position-state-changed-p
               before)
        (my/navigation-history--follow-back-location))))

  (defun my/navigation-history-forward ()
    "移動先ウィンドウへ進み、なければ同一ウィンドウの履歴を進む。"
    (interactive)
    ;; ウィンドウをまたいで戻った直後は、まず元のウィンドウへ進む。
    ;; その後はBetter Jumperの同一ウィンドウ内履歴を使用する。
    (unless (my/navigation-history--follow-forward-location)
      (better-jumper-jump-forward)))

  (defun my/navigation-history-register-window-transition
      (source-window source-marker destination-window)
    "SOURCE-WINDOWからDESTINATION-WINDOWへの移動を履歴へ登録する。
SOURCE-MARKERは移動前の正確なカーソル位置を指す。"
    (let ((source-location
           (my/navigation-history--location-at-window
            source-window
            source-marker)))
      (when (and source-location
                 (window-live-p destination-window)
                 (not (eq source-window destination-window)))
        ;; 新しいウィンドウには分割元のBetter Jumper履歴を持ち込まない。
        (better-jumper-clear-jumps destination-window)
        (my/navigation-history--set-window-location
         destination-window
         my/navigation-history--back-location-parameter
         source-location)
        (my/navigation-history--clear-forward-location
         destination-window))))

  (use-package better-jumper
    :ensure t
    :demand t

    :custom
    ;; 同じウィンドウ内で、ファイルをまたぐ履歴を管理する。
    (better-jumper-context 'window)

    ;; 新しく分割したウィンドウへ、分割元の履歴をコピーしない。
    (better-jumper-new-window-behavior 'empty)

    ;; 戻ったあとに別の場所へ移動した場合は、古い進む履歴を破棄する。
    (better-jumper-add-jump-behavior 'replace)

    :bind
    (("<mouse-4>" . my/navigation-history-backward)
     ("<mouse-5>" . my/navigation-history-forward))

    :config
    (better-jumper-mode 1)

    ;; すべてのコマンドを監視するが、履歴へ追加するのは
    ;; 明示的なジャンプか、同じウィンドウでのファイル切り替えだけ。
    (add-hook 'pre-command-hook
              #'my/navigation-history--before-command)
    (add-hook 'post-command-hook
              #'my/navigation-history--after-command))
#+end_src

** Avy：画面内の任意位置へジャンプ

画面内に表示されている文字列を指定し、
表示されたラベルを入力して、その位置へ移動します。

- =C-;=: 入力した文字列の位置へジャンプ

#+begin_src emacs-lisp
  (use-package avy
    :ensure t

    :bind
    (("s-t" . avy-goto-word-1)))
#+end_src

** Move Text：行・選択範囲の上下移動

VS Codeと同様に、Option＋上下キーで
現在行または選択中の複数行を上下へ移動します。

#+begin_src emacs-lisp
  (use-package move-text
    :ensure t

    :bind
    (("M-<up>"   . move-text-up)
     ("M-<down>" . move-text-down)))
#+end_src

** アンドゥ・リドゥ

#+begin_src emacs-lisp
  (use-package undo-fu
    :ensure t

    :bind
    (("s-z" . undo-fu-only-undo)
     ("s-Z" . undo-fu-only-redo)
     ("s-X" . undo-fu-clear-all)))
#+end_src

** 独自の編集コマンド
*** 現在の行を選択
#+begin_src emacs-lisp
(defun my/select-current-line ()
  "カーソル行全体を選択し、カーソルを次の行頭へ移動する。
 続けて実行すると、選択範囲を次の行まで広げる。"
  (interactive)
  (if (and (region-active-p)
           (eq last-command 'my/select-current-line))
      ;; 連続実行時は、選択範囲を次の行まで広げる。
      (goto-char (line-beginning-position 2))

    ;; 最初の実行では、現在行の先頭から次の行頭までを選択する。
    (let ((start (line-beginning-position)))
      (goto-char (line-beginning-position 2))
      (set-mark start)
      (activate-mark))))

;; VS Code風にカーソル行を選択する。
(keymap-global-set "s-l" #'my/select-current-line)
#+end_src

*** カーソル行の上下に新しい行を挿入

VS Codeの「Insert Line Below」「Insert Line Above」に相当する操作です。
カーソルが行の途中にあっても、現在行の内容は分割しません。

- =Cmd+Enter=: カーソル行の下に新しい行を追加し、その行へ移動する
- =Cmd+Shift+Enter=: カーソル行の上に新しい行を追加し、その行へ移動する

#+begin_src emacs-lisp
(defun my/insert-line-below ()
  "カーソル行の下に新しい行を挿入し、その行へ移動する。"
  (interactive)
  (end-of-line)
  (newline-and-indent))

(defun my/insert-line-above ()
  "カーソル行の上に新しい行を挿入し、その行へ移動する。"
  (interactive)
  (beginning-of-line)
  (open-line 1)
  (indent-according-to-mode))

;; VS Code風に、現在行の下へ新しい行を挿入する。
(keymap-global-set "s-<return>" #'my/insert-line-below)

;; VS Code風に、現在行の上へ新しい行を挿入する。
(keymap-global-set "S-s-<return>" #'my/insert-line-above)
#+end_src

*** 現在行の削除

VS Codeに近い行単位の操作を追加します。

- =Cmd+Shift+k=: カーソル行を削除する
#+begin_src emacs-lisp
(defun my/delete-current-line ()
  "カーソルのある行を、Kill Ringへ入れずに削除する。"
  (interactive)
  (delete-region
   (line-beginning-position)
   (line-beginning-position 2)))

(keymap-global-set "s-K" #'my/delete-current-line)
#+end_src
*** 行・選択範囲の複製
#+begin_src emacs-lisp
  (defun my/line-block-bounds ()
    "現在行、または選択範囲を含む行全体の範囲を返す。"
    (if (use-region-p)
        (let ((beg (region-beginning))
              (end (region-end)))
          (cons
           (save-excursion
             (goto-char beg)
             (line-beginning-position))
           (save-excursion
             (goto-char end)
             ;; 選択範囲の末尾が行頭の場合、その行は含めない。
             (if (and (bolp) (> end beg))
                 end
               (line-beginning-position 2)))))
      (cons (line-beginning-position)
            (line-beginning-position 2))))

  (defun my/duplicate-lines (direction)
    "現在行または選択した行をDIRECTION方向へ複製する。"
    (let* ((region-active-p (use-region-p))
           (original-point (point))
           (original-mark (and region-active-p (mark)))
           (bounds (my/line-block-bounds))
           (beg (car bounds))
           (end (cdr bounds))
           (text (buffer-substring beg end))
           (point-offset (- original-point beg))
           (mark-offset
            (and region-active-p
                 (- original-mark beg)))
           (ends-with-newline-p
            (and (> end beg)
                 (eq (char-before end) ?\n)))
           copy-beg)

      (atomic-change-group
        (pcase direction
          ('down
           (goto-char end)

           ;; ファイル末尾に改行がない場合は、行を分離する。
           (unless ends-with-newline-p
             (insert "\n"))

           (setq copy-beg (point))
           (insert text))

          ('up
           (goto-char beg)
           (setq copy-beg (point))
           (insert text)

           ;; ファイル末尾に改行がない行を上へ複製する場合も分離する。
           (unless ends-with-newline-p
             (insert "\n"))))

        ;; 複製された側へカーソルと選択範囲を移動する。
        (if region-active-p
            (let ((new-point (+ copy-beg point-offset))
                  (new-mark (+ copy-beg mark-offset)))
              (goto-char new-mark)
              (set-mark (point))
              (goto-char new-point)
              (activate-mark))
          (goto-char (+ copy-beg point-offset))))))

  (defun my/duplicate-lines-up ()
    "現在行または選択した行を上へ複製する。"
    (interactive)
    (my/duplicate-lines 'up))

  (defun my/duplicate-lines-down ()
    "現在行または選択した行を下へ複製する。"
    (interactive)
    (my/duplicate-lines 'down))

  (global-set-key (kbd "S-M-<up>")
                  #'my/duplicate-lines-up)

  (global-set-key (kbd "S-M-<down>")
                  #'my/duplicate-lines-down)
#+end_src
** グローバルキーバインド

*** Emacs設定・Orgコマンド

Emacs設定操作、Org Capture、Org Agendaの入口となるキーを設定します。
#+begin_src emacs-lisp
;; C-c eをEmacs設定操作用のプレフィックスキーにする。
(defvar-keymap my/emacs-config-map
  :doc "Emacs設定ファイルを操作するキーマップ。"
  "i" #'my/open-init-org
  "t" #'my/tangle-init-org
  "r" #'my/reload-init-file)

(keymap-global-set "C-c e" my/emacs-config-map)

;; Org Captureを開く。
(keymap-global-set "C-c c" #'org-capture)

;; Org Agendaを開く。
(keymap-global-set "C-c a" #'org-agenda)
#+end_src
*** 基本操作

#+begin_src emacs-lisp
  ;; macOS風の保存。
  (keymap-global-set "s-s" #'save-buffer)

  ;; macOS風の全選択。
  (keymap-global-set "s-a" #'mark-whole-buffer)

  ;; macOS風のコピー。
  (keymap-global-set "s-c" #'kill-ring-save)

  ;; macOS風の切り取り。
  (keymap-global-set "s-x" #'kill-region)

  ;; macOS風の貼り付け。
  (keymap-global-set "s-v" #'yank)

  (defun my/close-current-buffer ()
    "複数Tab Barでは現在のTab Barから外し、
  最後の1つのTab Barではバッファ自体を削除する。"
    (interactive)
    (if (> (length
            (funcall tab-bar-tabs-function))
           1)
        (bufferlo-bury)
      (kill-current-buffer)))
  (keymap-global-set "s-w" #'my/close-current-buffer)

  ;; 複数ウィンドウなら現在のウィンドウを閉じ、
  ;; 1ウィンドウかつ複数Tab Barなら現在のTab Barを閉じる。
  (keymap-global-set "s-W" #'my/close-window-or-tab)

  ;; Cmd+←：行頭へ移動する。
  (keymap-global-set "s-<left>" #'move-beginning-of-line)

  ;; Cmd+→：行末へ移動する。
  (keymap-global-set "s-<right>" #'move-end-of-line)

  ;; Cmd+↑：バッファの先頭へ移動する。
  (keymap-global-set "s-<up>" #'beginning-of-buffer)

  ;; Cmd+↓：バッファの末尾へ移動する。
  (keymap-global-set "s-<down>" #'end-of-buffer)

  (defun my/delete-to-line-beginning ()
    "カーソル位置から行頭までを削除する。"
    (interactive)
    (delete-region (line-beginning-position) (point)))

  (defun my/delete-to-line-end ()
    "カーソル位置から行末までを削除する。"
    (interactive)
    (delete-region (point) (line-end-position)))

  ;; カーソル位置から行頭までを削除する。
  (global-set-key (kbd "s-<backspace>")
                  #'my/delete-to-line-beginning)

  ;; カーソル位置から行末までを削除する。
  (global-set-key (kbd "s-<delete>")
                  #'my/delete-to-line-end)

  (defun my/delete-word-forward (arg)
    "カーソルからARG語先までを、Kill Ringへ保存せずに削除する。"
    (interactive "p")
    (let ((start (point)))
      (forward-word arg)
      (delete-region start (point))))

  (defun my/delete-word-backward (arg)
    "カーソルからARG語前までを、Kill Ringへ保存せずに削除する。"
    (interactive "p")
    (my/delete-word-forward (- arg)))

  ;; Option+Backspace：カーソル左側の単語を削除する。
  (keymap-global-set "M-DEL" #'my/delete-word-backward)

  ;; Option+Delete：カーソル右側の単語を削除する。
  (keymap-global-set "M-<delete>" #'my/delete-word-forward)
#+end_src
* 生成ファイルのフッター

生成される =init.el= の末尾部分です。
#+begin_src emacs-lisp
;;; init.el ends here
#+end_src
